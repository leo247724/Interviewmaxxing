# S3R integration corrections

Implement after S3 54a13d2. Own apps/service/** and tests/service/** only, plus explicit dependency merge commits. Do not edit other workers' packages, apps/web, root lock or private data. All tests use fictional temporary homes and localhost ATS; no real applications or provider calls.

1. Candidate isolation: native reviewer reproduced candidate B reading candidate A's selection with stale=false and linking it into B's pipeline. LocalSelectionBackend.latest/get and JobsApi must pass/check candidate identity. J2R is actively changing SelectionStore cache/history/latest/get APIs to require candidate_id. Integrate final J2R when available, or publish precise adapter dependency. Never accept another candidate's result merely because listing id matches.
2. Durable decision lifecycle: POST decide currently returns 202 without a pollable task. Late FAILED and restart INTERRUPTED appear only in service SQLite; cached same-result-id completion also leaves UI polling indefinitely. Expose ListingView.decisionTask = {id, state: QUEUED|RUNNING|SUCCEEDED|FAILED|INTERRUPTED, error: string|null, resultId?: string|null}; prefer the exact existing task vocabulary if different and immediately document it. Listing GET must expose terminal failure/interruption and completion even if selection id is unchanged. Test in-process failure, restart, cache hit, and active coalescing. Frontend Astra is waiting for exact DTO.
3. Apply Austin-first ranking before the 500 listing limit. J1 list_listings has rank_for; current service trims newest first, discarding older Austin onsite listings for remote ones. Honor full canonical preferences including state and US eligibility; J1R also fixing region matching.
4. Pipeline LinkedApplicationView must expose confirmationMethod and confirmationAuthority, using the same precise enum strings as application receipt DTO. A USER_CONFIRMED receipt is user-reported regardless of old site evidence. Never let a user report of no submission unlock an uncertain application.
5. ListingSourceView must carry postingUrl from canonical posting_url. sourceUrl remains source/search provenance. Job-specific postingUrl is the fallback navigation/application link after verified applicationUrl, never shared search sourceUrl. Add service contract tests; frontend owns consumption.
6. Remove repeated pipeline scans inside each listing: reuse the map already loaded. Prefer latest-only and batched candidate-scoped selection lookup, avoiding full decision-history parsing per card. Keep this bounded; indexed queue/cursor architecture is a later performance package.

Dependencies: J1S 1d8f2ee already fixes prior seed-test changes; J2S 0f58421 already supplies role_focus. Further J1R/J2R are under correction. P1R2 e5c14c7 approved. C4R3 and O1R browser and I1R core corrections are still pending. Do not claim final end-to-end until their final checkpoints are integrated and tested.

Frontend sole owner: native /root/frontend_astra_max in build/dashboard. Service may publish contract/readiness immediately, not write frontend. Final acceptance must run actual HTTP -> runner -> owned Chromium -> mock ATS, then coordinate dashboard-driven acceptance. Preserve TEST_ONLY default and candidate JSON pending. No unneeded connector auth or global upgrades.
