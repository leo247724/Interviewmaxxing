# C4R — Release-blocking browser corrections

Owner: browser-ats. C4 allowlist only. Independent review of clean bb1bb3a ran 114 passing browser tests and then reproduced seven defects with fictional routed localhost pages. Fix all seven, add meaningful real-Chromium regressions and preserve the generic driver seam. No fixture-specific runtime shortcuts. The task must commit and report exact checks/commit before integration. Do not use the user's OpenCLI tab or real employer pages for these probes.

1. `runtime.py:328–331`: fill stamps the post-fill fingerprint, authorizing newly revealed questions. Repro email-only valid packet; email input handler appends required checked checkbox name=attest value=yes with label “I certify that I have never been dismissed for misconduct.” Fill says ok despite packet.problems_against(after) flagging changed fingerprint and unanswered attestation; submit POST includes attest=yes. Keep packet-approved fingerprint; new/changed questions require fresh inspection/resolution, and uncovered newly prechecked consent must never be submitted.
2. `runtime.py:598–610`: repeated form plus any role=alert becomes NOT_SUBMITTED/FILLING. Repro final POST returns HTTP502 with same form and alert “The connection timed out. We cannot confirm whether your application was submitted.” Confirm permits retry, producing two POSTs. Transport/generic ambiguous errors must remain UNKNOWN; only definite rejection with sufficient evidence can unlock retry. Preserve safe explicit native/field validation handling.
3. `signals.py:18–24` + `runtime._acceptance:567–590`: negative wording “No application received” plus matching Job ID ABC-123 yields ACCEPTED on reconcile. Require affirmative non-negated acceptance; cover no/not/never/conditional instructions and lookalike text.
4. `_acceptance:585–587` reused by `reconcile:690–702`: reconciling Widget Engineer/ABC-123 on portal “Application submitted / Role: Graphic Designer / Reference: APP-9876” yields ACCEPTED from unseen reference only. A fresh reference can tie an immediate post-click result, not an arbitrary record on a later portal. Reconciliation must identify the requested application/job and avoid mixing text across multiple records.
5. `_lookup:724–738`: form method=get is checked but button formmethod/formaction overrides ignored. Repro status form method=get action=/lookup, email input, submit button formmethod=post formaction=/application; reconcile sends POST /application. Validate the effective method and target including overrides immediately before lookup; never dispatch potentially mutating application actions during reconciliation.
6. `semantics.py:131–146`: attestation recognition limited to single CHECKBOX. Required RADIO legend “I certify that I have never been dismissed for misconduct.” with Yes/No options becomes CUSTOM_SELECT; candidate-fact provenance passes core checks. Recognize consent/attestation language across applicable control types (radio, select, text where appropriate) so core explicit-answer rules apply.
7. `inspect.js:113–118`: all BUTTON elements skipped before custom-widget roles. Required button type=button role=combobox aria-label=Preferred office aria-required=true with text “Select an office” disappears, allowing final submit. Include relevant button custom-widget roles, report unsupported needs and block submission until genuinely operated; ordinary action buttons remain actions.

Use these exact minimal probes and add negative/positive cases that distinguish actual acceptance, explicit rejection, and uncertainty. Test both immediate confirmation and later reconciliation. Existing suite passing is not sufficient. Run full browser tests, scoped lint/types, preserve no repeat dispatch after uncertainty. Return SUPERSET_WORKER_DONE task C4R with exact commit and limitations. After review, O1 OpenCLI driver is a follow-up; do not broaden this correction.
