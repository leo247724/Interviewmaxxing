# Bounded worker assignment

- Task/run ID and objective:
- Role; requested model; pool/child-agent budget:
- Workspace and authoritative task/terminal handle:
- Current scope, constraints, and existing authorization:
- Inputs to read now (paths/hashes); references to load only if needed:
- Allowed writes and exclusive source/browser lease:
- Independent reviewer / consumer and handoff destination:
- First useful deliverable and subsequent batch limit:
- Finite completion or exhaustion condition:
- Status/receipt path; actual UTC timestamps:
- What to report for a blocker; what may continue independently:
- Owned resource identities to release on completion:

Return artifact paths and checks, not full raw evidence. Do not expand the pool, change scope, or claim downstream saves merely because your own package is finished.
