# Dynamic application routing (prepare-only)

The AI package adds semantic interpretation to freshly inspected application fields and supplements the existing factual packet resolver. It has no browser actions, JavaScript execution, URL navigation, file selection or submission capability. The integration must stop at the final review step with submission disabled. A completed draft is **not submitted** and is not an application receipt.

```python
from pathlib import Path
from interviewmaxxing_browser.ai import build_ai_runtime

router, resolver = build_ai_runtime(
    env_file=Path("/absolute/worktree/env.local"),
    writer_model="anthropic/claude-opus-5.5",
)
# On every fresh canonical inspection, before packet resolution:
annotated = router.annotate(form, document_id=current_document_identity,
                            schema_hints=current_schema_prior)
# Build the canonical PacketContext using annotated, then:
packet = await resolver.resolve(context)
# Canonical executor/runner owns filling, reinspection and final-review stopping.
metadata = router.decisions.budget.metadata()
```

`annotate` is synchronous; an async browser integration should run it with `asyncio.to_thread`. `resolve` is async and moves its provider work to a thread. The factory reads only `OPENROUTER_API_KEY` through the existing selection credential loader with an explicit env-file path. Neither receipts nor exceptions expose the key or raw profile values.

## Full-form routes, binding and provenance

- `classify_form(form, document_id=..., schema_hints=...)` returns a typed `FormRouteReport`; `annotate` preserves the existing interface and `report_for(form)` is a non-provider lookup for the exact current inspection. Every field is evaluated, including native email and known identity controls. Decisions expose route, semantic type, prose need, source applicability, and their separate confidences/probabilities. FILE controls add attachment-versus-autofill purpose.
- The router receives the actual `ApplicationForm` from the generic DOM inspector. It only updates `semantic_type` for custom/unknown fields. It preserves IDs, selectors, option values, disabled state, constraints, scope and inspection time. Known sensitive semantic types are never downgraded.
- Page wording and schema priors appear only in decision state, not trusted instructions. A schema prior cannot add a field or select a locator. The entire current form binding, document identity, schema contents, prompt version and model contribute to the request/cache identity. A cache entry contains typed decisions; applying it still starts with the current form.
- Annotation does not establish document freshness. The caller must re-inspect after DOM changes and retain the canonical stale-packet, per-field binding, file verification and page-document checks. The existing canonical form fingerprint excludes semantic metadata (also some changing constraints); the AI hash includes all current binding metadata. Neither hash authorizes an action by itself.
- The factual resolver proposes answers first. Exact known identity/current values are released only when the whole-form gate confirms the requested subject is the applicant and the current profile source applies. An email input does not establish whose email is requested: supervisor, reference, employer and historical questions cannot use the applicant profile shortcut. A Jev choice may identify only an offered verified fact. Its value is then read from the candidate object and translated through existing typed value/option validation. Jev never generates the copied value. Conflicting same-key facts, unavailable facts and mismatched/disabled options hold. Unverified facts are excluded from provider requests.
- Existing scoped user inputs and matching saved answers retain precedence, including unusable saved answers. Explicit consent, attestation, eligibility, salary and protected attributes cannot enter AI supplementation. Unknown classifications, missing facts, unsupported/file controls and provider failures retain missing inputs. Approved document attachment fields remain the canonical resolver/executor's responsibility. Resume parser/autofill controls are held because their side effects can overwrite other answers; they are not equivalent to attachments.
- Every returned packet passes `PacketContext.problems`, preserving source IDs, candidate/job scope, required-field coverage and control compatibility. Model output cannot supply an arbitrary candidate ID, file path or browser command.

## Option equivalence, referral policy and lookup choice

These three decisions map the user's own stored answer onto what a site offers. Saved answers still apply only to the exact question wording they were given for, and Jev never produces a value. Each decision is one Jev Choice over opaque keys; the option and suggestion text travels in `state` as data (prompt version `option-choice-v1`). A pick is used only when its confidence is at least 0.90 (`MIN_CONFIDENCE`) and its probability at least 0.95 (`MIN_PROBABILITY`). Traces in `narrative_traces` carry field ids, keys, scores, the rule and the status, never the stored answer, option labels or typed text. Budget receipts use the purposes `option_equivalence`, `referral_policy` and `lookup_suggestion`.

- **Option equivalence.** A choice control (select, radio, multi-select, checkbox group) that has no answer yet goes to Jev when a saved answer for exactly its wording applies, or a verified identity value applies to an identity field, but no enabled option matches under the deterministic rule (normalized label, plus country and US-state spellings). The user's own input for the question is never remapped. Jev chooses among the enabled options plus `NONE`: the option whose meaning is identical to the stored answer, neither broader nor narrower, with the same yes/no polarity. A stored "Yes" to the work-authorization question becomes "Yes, I am authorized to work in the US", and wording variations of the same meaning do not change the pick. An option that adds a condition, or has the opposite polarity, is `NONE` and the question keeps its hold. For a multi-choice answer, each stored item without an exact option is one question in the same request, and all of them must pass. This is allowed for every semantic type, including the explicit-answer ones (work authorization, sponsorship, EEO), because the value remains the user's saved answer: provenance stays `SAVED_ANSWER` with its ids, or `PROFILE_IDENTITY`, which still needs the `COPY_KNOWN` applicant-current gate. The confidence is the lower of the two scores, and the option must pass `answer_problems` and the final `PacketContext.problems`.
- **Referral source.** "How did you hear about us?" is never held when a referral-source saved answer applies. That can be a saved answer to the exact wording, or, for a field typed `REFERRAL_SOURCE` by the inspector or by Jev's annotation, the user's standing referral answer (the simple-answers default "Company career page"). One Jev Choice encodes the owner's order in its keys: `careers_oN` for the company's own careers page, careers site, website or job posting (rule 1), `other_oN` for "Other" (rule 2), and `board_oN` for a job board or LinkedIn (rule 3). `NONE` means a how-did-you-hear question with none of these, which leads to rule 4, the first enabled real option, chosen by deterministic code and never by the model. Rule 4 also applies when Jev cannot settle between options but leaves at most 0.05 on `NOT_SOURCE`. `NOT_SOURCE` marks a question that is not about the channel (who referred you, a referrer's name, whether an employee referred you), because the inspector types anything with "referr" as referral source. Such a field is never auto-answered; it can still get an ordinary equivalence mapping from a saved answer to its exact wording. The answer cites the referral saved answer, and its note records the rule. If Jev is unavailable, the guard cannot run and the question keeps its hold. The full-form classifier (`full-form-routing-v10`) now describes the referral-source category as the channel the applicant came from, not a referrer.
- **Lookup choice.** For a lookup the site could not commit (`NEEDS_CHOICE`), the runner calls `DynamicPacketResolver.choose_suggestion(context, field, typed_value, suggestions)`. Jev chooses among the observed suggestions plus `NONE`: the suggestion that denotes exactly what the typed value states, which is the candidate's own city with the same state or region and country. "Austin, TX" picks "Austin, Texas, United States", not "Austin, Minnesota, United States". A same-named city elsewhere, a broader or narrower place, a split or low-scoring decision, or a provider failure returns None, and the user then picks from the listed suggestions. For a city, state, country or location lookup the decision also sees the applicant's verified city, region and country (`applicant_address`), and a suggestion must be in that region and country. A city lookup types the bare city first. Some sites (live Greenhouse) offer nothing for "Austin, TX" but suggestions for "Austin". The decision then sees the applicant's region, so "Paris" typed for an applicant in Paris, TX does not pick "Paris, France", and when no suggestion is in the applicant's region the person picks. Only when the site offers no suggestion at all for the bare city does the runner type "City, Region" once (`lookup_alternatives`, round 7). `suggestion_decision(field_id)` returns the decision metadata, which the runner stores in the `field.suggestion_chosen` event. The deterministic `FactualPacketResolver` never chooses.
- **Reworded questions (round 2).** The first path is always a saved answer to the exact wording. If there is none and no user input, a required short-answer field whose type is reusable gets a second path: work authorization, sponsorship, referral source, EEO, location, school and degree, which are the types the simple-answers map writes. A custom or unknown field can take an untyped saved answer, such as the age-18 or employee-referral answer. The candidates are the user's GLOBAL saved answers of that type plus untyped ones; job-scoped answers never take part.
  - One Jev Choice (question `wording`, purpose `question_equivalence`) compares the observed label, help text, placeholder, section context and options with each saved question's wording and variants. The values are never sent.
  - The pick must ask exactly the same thing: the same person, timeframe, yes/no polarity and answer type. A question that adds or drops a condition, or asks for a different status ("which authorization do you hold" versus "are you authorized"), is `NONE`.
  - The gates are confidence 0.90 and probability 0.95. The probability is summed over saved questions that carry the same answer, since any of them gives that value.
  - The saved value is then used as if its wording had matched: an exact translation, else option equivalence. Provenance is `SAVED_ANSWER` with the saved-answer ids and the note "question wording mapped by Jev".
  - Explicit-answer types still come only from a saved answer.
- **Yes/no experience screeners (round 2).** A required question that the full-form gate routes as a literal `COPY_KNOWN` answer about the applicant's own history (`HISTORICAL_OR_CONTEXTUAL`, gated) is answered from verified facts. Examples: "Do you have experience working at a digital marketing agency?" or "Do you have lifecycle or CRM experience?". It qualifies as a radio or select with one yes-like and one no-like option (extras such as "Prefer not to say" allowed), or as a text field phrased as a yes/no question.
  - The facts come from the RAG retriever when configured, else from the verified list within the 40-fact bound.
  - One request (purpose `experience_screener`, prompt `experience-screener-v2`) holds the Choice `experience` (YES, NO, UNKNOWN, NOT_EXPERIENCE) and, per fact, nouls asking whether the fact explicitly states the named experience (`has_fN`) or its absence (`lacks_fN`).
  - YES needs the Choice at the gates and at least one fact stating the experience; NO needs a fact stating its absence. Absence of a fact is UNKNOWN, never NO.
  - Facts that point both ways hold as a conflict, and UNKNOWN holds with a prompt that names the question and the fact that would settle it.
  - A named tool or platform must be named in a fact, and the ABM-platform rule still requires platform names.
  - The evidence facts pass the same-key and canonical consistency checks. The answer maps to the site's yes/no option (exactly, else by option equivalence) or to the text "Yes"/"No". Provenance is `GENERATED_FROM_FACTS` citing the evidence facts.
  - `NOT_EXPERIENCE` falls back to the ordinary fact route.
- **Residence questions (round 3).** A required single-choice question about where the applicant lives is answered from the verified address. It must be typed location, country, state or city, and routed `COPY_KNOWN` with an applicant-current source. Examples: "Do you currently live in the United States?", "Do you reside in any of the following states: AL, AZ, CA, …?", "Which state do you reside in?", or a region select for "What is your current location?".
  - One Jev Choice (`residence`, purpose `residence_screener`) sees the question, the options and the applicant's city, region and country. It picks the option that is true for that address, including a region that contains it; a "Remote/Other" option only when it clearly applies. Otherwise it answers `UNKNOWN` or `NOT_RESIDENCE` (relocation, a previous residence, the job's location, citizenship), and the field is held.
  - When a yes/no question lists at least two US states and is not negated, code also checks the region's membership in that list; a disagreement holds.
  - The answer is `PROFILE_IDENTITY`, so it still passes the applicant-current route gate. A yes/no residence question skips the identity option-equivalence call, since an address never means "Yes"; other identity selects try it first.
  - The classifier describes location, country, state and city as the applicant's current residence, including yes/no and state-list questions. Its source-scope criteria count where the applicant currently lives as applicant-current, even when the answer decides eligibility.
  - Browser heuristics leave many residence questions custom-typed ("…any of the following states: …" says "states"), so the type comes from Jev's annotation. A split among the four residence types counts as one reading on a single-choice control; text controls are never pooled. The pooled gate is described under "Classifier robustness" (`full-form-routing-v13`), which replaces the round-4 details.
- **Current-address clarification (round 3).** A near-threshold applicant-current source score (0.90–0.95) on a city, state, country, location, ZIP or street field still gets the one full-form clarification call. Its accepted score is 0.90 instead of 0.95 when three things hold:
  - the classifier's remaining mass is only current-versus-historical (at most 0.01 elsewhere);
  - the question's own wording states the present ("currently", "now", "do you live/reside", "where are you located/based");
  - no previous, prior, former or past wording appears in the question or its section.
  A second identical call would come from the decision cache and would add no independent evidence, so one relaxed threshold is used instead. Previous-residence wording keeps the strict 0.95 path, and Jev still sees and rejects it.
- **Choice and numeric screeners (round 3).** A required single-choice question (not yes/no), or a numeric text question ("How many…", `type=number`), about the applicant's own experience is answered from verified facts. It must be routed as a literal `COPY_KNOWN` answer from the applicant's background or current work.
  - A choice (`fact_choice`) needs a fact that states the value, with a `states_fN` noul of at least 0.95. A range option must contain every amount the evidence states: "$400,000" falls in "$250K - $500K", and so does a stated range such as "$400K–$500K" (round 6). Bare years such as "2021" date a fact and are not amounts. Otherwise the field is held (`RANGE_MISMATCH`).
  - A number (`fact_value`) is copied only when the chosen fact states exactly one plain number. "About 25", "25+" and ranges are held.
  - Nothing is estimated, rounded or converted. `UNKNOWN` holds with a prompt naming the fact that would settle it, and `NOT_EXPERIENCE` falls back to the ordinary fact route. Provenance is `GENERATED_FROM_FACTS`, or `CANDIDATE_FACT` for a numeric fact copied as is.
- **Resume with autofill (round 3).** A required file control that is the resume (by type or by a resume/CV label) is the approved attachment even when Jev also marks it as an autofill/parser upload. The purpose must be attachment or autofill with a combined probability of at least 0.95. The report field carries `autofill: true`, and `narrative_traces` gets a `resume_upload` entry. The browser uploads it first and re-inspects, so later fields are filled from verified values. Optional or non-resume parser controls stay unsupported, and a missing resume file is never invented.
- **Multi-select answers (round 4).** A select-all question (multi-select or checkbox group) gets every option that a stored answer or the verified facts explicitly support, and nothing else.
  - A stored text answer ("US Central, US Eastern"), from exact wording or a reworded GLOBAL answer such as `available_time_zones`, gets one per-option noul `includes_oN` (purpose `multi_select`). A reworded answer may be broader than the listed options, for example all time zones versus U.S. time zones; it is then filtered to them.
  - A required select-all question about the applicant's own experience ("Which platforms have you managed?") is fact-grounded. One request holds the Choice `fact_select` (SUPPORTED/UNKNOWN/NOT_EXPERIENCE) and, per option, a Choice `source_oN` over the supplied facts plus NONE: the fact that explicitly states that option. An option is selected when NONE has at most 0.05 probability with confidence at least 0.90, and left out on a confirmed NONE. The answer is `GENERATED_FROM_FACTS` citing, for every selected option, the fact Jev named for it; the trace records `sources` by option.
  - Any undecided option (between 0.05 and 0.95), or no supported option, holds. "Other", "None", "N/A", "All of the above" and decline options are never chosen this way.
  - When Jev confirms that a reworded saved answer asks this question but its value does not fit the options (an undecided option included), the field holds with an `AMBIGUOUS` prompt naming the saved question. It never falls through to a fact screener or the writer, so the user's own answer is never replaced by a partial generated one.
  - EEO select-all questions stay on the explicit path, answered only from saved answers or user input.
- **Consistency precision (round 4).** The fact-consistency check compares only facts that can be about the same subject: global or negative claims, one non-additive key with two values, the same experience group, or ungrouped bullets naming the same employer, client, project or tool. Its Jev verdicts are cached per runtime by exact fact and comparison set (see [rag-writing.md](rag-writing.md)).
- **Bare contact fields (round 6).** The applicant's own first, last, full or preferred name, email, phone, or LinkedIn/GitHub/website URL is copied from the verified identity without the source-scope gate or the clarification call. This is because Jev's hedge on these fields (live: 0.92 applicant-current with 0.08 unclear, or 0.74 with 0.23 explicit-answer) held identical fields on one form and copied them on another. All of these must hold:
  - the route is `COPY_KNOWN`;
  - the control is a text input (text, email, tel or url, or the phone widget), never a text area or a choice control such as "Preferred contact method";
  - the label is a bare contact wording for that type ("First Name", "Email Address", "Mobile number", "LinkedIn Profile URL"…, compared with `wording_key`);
  - nothing in the label, help text, placeholder or section names another person: reference, referee, supervisor, manager, employer, emergency, recruiter, agency, spouse, partner, parent, guardian, contact person, their, his or her;
  - nothing there names a past or other identity (see the guard words under "Narrative escalation" below);
  - at most 0.01 of the source mass is on another person or entity. Unclear and explicit-answer mass are ignored: a bare "Email" on an application is not a decision.

  The confidence is at most 0.99 and the route's own score. The trace status is `APPROVED_BARE_CONTACT`. Every other identity field keeps the gates below.
- **Route mass instead of the route label (round 6).** A split route such as `COPY_KNOWN` 0.94 / `HUMAN_INPUT` 0.06 makes the label `AMBIGUOUS`. That used to hide residence questions and experience screeners from their deciders. When the label is `COPY_KNOWN`, `AMBIGUOUS` or `HUMAN_INPUT`, at most 0.01 of the route mass is on `WRITER`, `UNSUPPORTED` and `APPROVED_DOCUMENT`, and the source scope passes its own gate (at least 0.95 at confidence 0.90), then:
  - a residence question goes to the residence screener, and its answer passes the route gate;
  - a yes/no experience question goes to the experience screener;
  - a choice or numeric experience question goes to the fact screener.

  The decider's own ≥ 0.95 decision, and the code state-list and range checks, remain the safety gate. A field admitted this way that is not in fact a screener keeps the hold it had; it never goes to the general fact route.
- **Relocation questions that name a place (round 6).** A required relocation select or radio that names a US state or lists state options is handled in three steps. Examples: "Do you currently reside in or will you be relocating to any of the following states?" and "This position is located in Austin, Texas. Are you currently located in Austin or plan to relocate to Austin?".
  1. The relocation screener runs first. One Jev Choice (`relocation`) sees the verified address and picks the option that is true because the applicant already lives there, or `UNKNOWN`, or `NOT_PLACE`. Code requires a yes/no answer to be Yes, and the applicant's state to be among the named states or options. The source scope is not required, because relocation reads as a preference. Core allows this address-derived single choice as `PROFILE_IDENTITY` on relocation fields (`ADDRESS_DERIVED_TYPES`).
  2. Otherwise the user's saved relocation answer (`willing_to_relocate`) is mapped onto the options.
  3. Otherwise the field is held.
- **Saved answers of the field's own type (round 6).** A typed field is offered only the GLOBAL saved answers of its own type when it has any. Untyped answers such as education dates, referral or above-18 would spread Jev's mass. Untyped answers are offered only when no same-type answer exists. The wording decision also treats "this company" in a saved question as the employer the application is for, and treats desired salary, base salary, compensation and pay expectations as one question. Current or past pay, bonus only and equity only are not the same question. Choice prompts are `option-choice-v2`.
- **Malformed decisions and multi-select referral (round 6).** A Jev response that fails validation (`MALFORMED_RESPONSE`) is retried once with the same request, as a second budgeted call, before the decision holds. For a select-all "How did you learn about us?" the referral policy picks exactly one option under the same rules.
- **Provider cost receipts (round 4).** `DynamicPacketResolver.provider_usage(since)` totals the runtime's receipts after a mark (calls, known cost, unknown-cost calls and latency, overall and by purpose). The runner records one `provider.budget` event per run, under its claim, and adds only the run's total to the outcome message ("Provider cost: USD 0.0123 for 14 call(s)."). Usage before a submit or a first job-identity binding is recorded first in its own event, because a submission outcome or a duplicate job releases the claim. The batch ledger and `batch-report` read these events ([mass-preparation.md](mass-preparation.md)).

## Classifier robustness (round 7, `full-form-routing-v13`)

Pilot 7 (40 fresh forms, prepare-only) lost fields to three classifier rules, which this round changes. Where they differ, these rules replace the pooling details under "Residence questions" and "Resume with autofill" above and the batching sentence under "Bounds and observations".

- **Pooled gates read pooled mass.** Two gates pool choices that later code treats alike: the residence types (location, country, state and city) on a single-choice control, and a required resume's attachment and autofill-parser purposes. A pool passes when its combined probability is at least 0.95 and no single choice outside it exceeds 0.03 (`RouteThresholds.max_pool_outside_probability`). Jev's per-choice confidence is not read, because a split between two pooled choices lowers it although the reading is the same.
  - Greenhouse's "Do you currently reside in the US?" (COUNTRY 0.24, LOCATION 0.71) is now typed location and answered by the residence screener. Ashby's required "Resume / or drag and drop here" is the approved attachment again.
  - The decision records `semantic_pool_share` (fields whose meaning Jev classified and that a pool can decide) and `document_pool_share` (file controls). `semantic_confidence` and `document_purpose_confidence` keep Jev's raw values.
  - Text controls are otherwise never pooled. A resume purpose with more than 0.03 elsewhere falls back to the ordinary per-choice attachment gate (confidence 0.90 and probability 0.95).
  - Round 2, from the v13 re-check:
    - **Yes/No residence questions.** On Yes/No options, a CUSTOM_BOOLEAN reading is the control's shape, not a competing meaning. It joins the residence pool while the residence types together outweigh it. Greenhouse's LOCATION 0.75 / CUSTOM_BOOLEAN 0.13 / COUNTRY 0.11 pools at 0.99 and takes location. A competing custom reading such as CUSTOM_TEXT still counts as outside, and so does CUSTOM_BOOLEAN on other options.
    - **Address text boxes.** A text box whose label asks for an address pools ADDRESS with LOCATION into ADDRESS, so "What is your current home address?" at ADDRESS 0.93 / LOCATION 0.05 is the address. The factual resolver copies the identity street for it.
    - **Required resumes on a sure route.** When Jev's APPROVED_DOCUMENT route mass is at least 0.95, a required file control typed or labelled resume is the approved attachment whatever the purpose reading. Ashby's traced 0.52 / 0.37 / 0.11 purpose split (pool 0.89) no longer holds it. Optional file controls keep the purpose gate, so a parser-only helper is never attached.
      - Round 4 adds a label check. The heuristics type any upload label with "resume" or "CV" as the resume, but a label that only mentions it is not the resume. That covers a label naming another document (transcript, portfolio, cover letter, sample, references, recommendation, certificate, diploma, licence) or negating the resume ("Transcript (do not attach your resume)", "Portfolio or resume").
      - Such an upload is never approved as the resume, and its RESUME type, from the heuristics or from Jev, becomes UNKNOWN. The resume is never attached to it, and a required one waits for the person.
    - `autofill` is set whenever the purpose is not confidently an attachment (confidence 0.90 and probability 0.95), because the browser uploads the file first and re-inspects the form.
- **Bounded requests never lose a form.** Each field lists at most 40 options (`MAX_FIELD_OPTIONS`) with its `option_count`. A longer list adds an `options_note` ("… and 210 more") and an `option_shape` hint from cheap checks of every option: `dial_codes`, `years`, `us_states`, `countries` or `other`.
  - Fields are packed in form order into requests of at most `batch_size` fields and 85% of `CallBudget.max_request_bytes`. Every request carries the same state: prompt version, observation, schema prior and the whole form.
  - A request that still exceeds the bound is halved, down to single fields; only a single field over the bound is held. If the whole-form state would take more than half of a request, fields list 10 options, then none; the count, note and shape stay.
  - `FormRouteReport` records `batches` and `options_per_field`. On pilot 7's Appspace form, a 240-option "Country / Phone" menu pushed every request over 60,000 bytes and held all 13 required fields. A synthetic form of the same shape is now classified in three requests.
  - Round 3 (Greenhouse, Pomelo): one of three batched calls ended in "Jev TIMEOUT" and held its six fields, First Name, Email and Phone among them. Two changes follow:
    - A request that times out is now sent once more as two halves of its fields (a single field as itself) before any of them is held. Only then does a half that fails again hold its own fields. `FormRouteReport.retries` counts the retried requests.
    - Full-form calls now use a per-call timeout of at least 60 s (`ROUTES_TIMEOUT_SECONDS`); the runtime's Jev client keeps 15 s for the other decisions.
    - The retry and the timeout share the runtime's budget and transport. The timed-out call and each half are separate reserved calls, all counted in `batches` and `provider_calls`. Other provider failures are not retried here.
    - Round 4: the router keeps one 60 s decisions instance per runtime, rebuilt only when the runtime's decisions or client change. The annotator and the resolver's fallback therefore share its single flight and response cache.
  - Round 5 (Rippling): **legal questions on choice controls.** "Is your authorization to work in the United States" with the options Permanent / Temporary and subject to expiration read WORK_AUTHORIZATION 0.86 / CUSTOM_BOOLEAN 0.08 / CUSTOM_SELECT 0.05: `UNKNOWN` under the per-choice gate, so the round-9 derivation never saw it. On a select or radio, a CUSTOM_BOOLEAN or CUSTOM_SELECT reading restates the control's shape, not another meaning, so both join the pool of the leading legal type (WORK_AUTHORIZATION, and SPONSORSHIP likewise, `LEGAL_TYPES`) the way the residence types pool. The field takes the legal type when the pooled share is at least 0.95 with at most 0.03 on any outside reading, and the legal type outweighs the shapes together (and the residence types, whose pool is read first). The Rippling field pools at 0.99, is typed work authorization and gets "Permanent" from the status table.
    - A split led by a shape (CUSTOM_SELECT 0.50 / WORK_AUTHORIZATION 0.40) stays `UNKNOWN` and holds, and so does one where the shapes together outweigh the legal type. The two legal types never pool with each other: WORK_AUTHORIZATION 0.60 / SPONSORSHIP 0.35 is two different legal answers, so the 0.35 counts as outside. CUSTOM_TEXT, UNKNOWN and every other reading count as outside too. Text and multi-select controls are never pooled.
    - `semantic_pool_share` records the legal pool whenever the leading legal type outweighs the shapes and the residence types, and the residence pool otherwise. Explicit-answer types are still never downgraded: a field the heuristics typed WORK_AUTHORIZATION is not asked its meaning.
  - Round 5 retry (the live prepare-only retry on the merged head; eight misses from the projected `routing.trace` types):
    - **Authorized to be employed.** The browser heuristics type "authorized to be employed", "employment authorization" and "eligible for employment" WORK_AUTHORIZATION, like "authorized to work", so Ashby's "Are you authorized to be employed in the United States?" is derived from the stated status (the table reads "employed" too).
    - **A Yes/No experience question is never a consent.** On a yes/no question whose wording asks about the applicant's own experience, skills or background ("Do you have experience working at a digital marketing agency?", "Have you worked in …", "years of", "familiar with", "proficient") and has no consent or attestation wording, a CONSENT or ATTESTATION reading is CUSTOM_BOOLEAN whatever Jev's route or source reading, and a CONSENT/CUSTOM_BOOLEAN split pools into CUSTOM_BOOLEAN. The plain-yes/no demotion (route and source gates) still covers the other yes/no questions. Consent wording keeps the explicit answer.
    - **Quantity text boxes.** A text box whose wording asks for a quantity ("How many …", "What is the largest overall annual ad spend you have personally overseen …"; `QUANTITY_QUESTION`, which the resolver's numeric fact screener now shares) and names no URL, link, site, profile or portfolio, with an input type other than `url`, takes no profile URL: WEBSITE, LINKEDIN and GITHUB readings pool with CUSTOM_TEXT into CUSTOM_TEXT, at 0.9 or at 0.97.
    - **A custom field Jev cannot name is what its control is.** When Jev's custom and unknown readings together reach 0.95 with no named type above 0.03, a field the heuristics typed custom keeps that type (CUSTOM_TEXT, CUSTOM_SELECT, CUSTOM_MULTISELECT; CUSTOM_BOOLEAN on yes/no options) instead of UNKNOWN. The time-zone select-all (`available_time_zones`), "County" (`county`) and the familiarity select-all (`familiar_with_company`) are typed so, and their untyped saved answers reach them: the county by its exact wording, the other two by the wording decision and then per-option inclusion (the familiarity answer is placed only when an option says what it says, else the field holds). A named type at 0.04 (CITY beside "County") still holds the field as UNKNOWN.
    - **Work-history company.** "Company name", "Employer name" and "Employer" are CURRENT_COMPANY (Paylocity's `company-name-0`); the verified current company answers the current entry, and the route gate holds an entry whose source reads as historical.
    - **Years of experience.** The heuristics read a count of years with up to four words between "years" and "experience" ("How many years of programmatic media experience do you have?"), not a yes/no "at least 8 years of … experience". A field the heuristics typed is not asked its meaning; its decision now records the heuristics' reading explicitly (`semantic_confidence` 1.0, all mass on the type) instead of None.
  - Round 6 (four live misses on the merged head):
    - **Minimum years of experience.** "Do you have 5+ years of hands-on paid media experience, including paid social, …" (Lever radio, Yes/No) read `UNKNOWN` 0.62, so no screener ran. A yes/no question ("Do/Have/Are you …") about experience with a minimum number of years is the yes/no experience screener, which routing answers from `years_experience.<area>`. The minimum can read "5+", "at least 8", "5 or more", "over 5", "more than 3", "a minimum of 4", a bare "3 years", or a spelled number.
      - The heuristics type such a question CUSTOM_BOOLEAN on a select or radio (`YEARS_THRESHOLD`). "Are you at least 18 years old?", a count ("How many years …"), a select-all and a text box keep their types.
      - The classifier reads a years count on Yes/No options as the screener: a sure YEARS_EXPERIENCE becomes CUSTOM_BOOLEAN, and so does a split, recording `demoted_from`. On a yes/no experience question, the years count and the reading that names nothing pool with the custom yes/no readings (`_EXPERIENCE_POOL`).
    - **The agency question under a consent heading.** "Do you have experience working at a digital marketing agency?" still reached routing as CONSENT, because the round-5 experience guard gave way to any consent wording, including the section headings above the question (a privacy notice heading, for example). Only the question's own label, help text, placeholder and options count now. A consent act there still keeps CONSENT, and so does a question with no experience wording under a consent heading. On a Yes/No question, the custom select reading (the options' own shape) also folds with CONSENT and custom boolean, so a CONSENT/CUSTOM_SELECT split is not `UNKNOWN`.
    - **Time zones.** The time-zone select-all reaches the free-text `available_time_zones` answer end to end, whether the classifier leaves it CUSTOM_MULTISELECT or `UNKNOWN`: the untyped wording gate maps the saved question, and `includes_oN` picks the options the text names. A live `NO_ANSWER` there means the wording step found no candidate (no saved value) or returned `NONE` / `BELOW_GATE`; the field's `question_equivalence` trace tells which.
    - **Work history.** Bare "Position", "Job title", "Position title" and "Position held" are CURRENT_TITLE, and "Company name" is CURRENT_COMPANY. The current role fills them when the entry's source is the applicant's current role; a past entry is held. A month-and-year date ("Start Date MM/YYYY") or a repeated block's indexed id (`start-date-0`) is never the availability START_DATE. No core type names an employment date, so "End Date MM/YYYY" stays a custom text field and waits for the person.
- **Consent and attestation only with their wording.** The semantic criteria now require the field's own wording to ask the applicant to consent, agree, acknowledge, authorize, give permission, certify, declare or sign. They also state that a yes/no question about the applicant's own experience is a custom boolean.
  - A post-check covers the inspector's heuristics too, whose consent rule matches "marketing". It demotes a question typed CONSENT or ATTESTATION, by the inspector or by Jev, to `CUSTOM_BOOLEAN` only when all of these hold:
    - it is a single-choice yes/no question;
    - its label, help text, placeholder, section headings and options have no consent or attestation wording;
    - Jev's own reading is one literal fact about the applicant: its proposed route is `COPY_KNOWN`, and its source mass on applicant-current plus historical reaches 0.95 with no explicit-answer, unclear or other-person reading above 0.03.
  - The source criteria already read a consent, decision or attestation as an explicit answer. So a real consent whose wording the check misses ("Marketing communications") stays CONSENT, and it can never take a differently worded saved answer.
  - The wording check looks for acts, not topics: agreeing, acknowledging, authorizing, permitting, certifying, declaring, attesting and signing; "I have read", "I understand" or "I confirm"; having read or reviewed a notice or policy; "true and complete"; opting in; receiving messages or being contacted; and keeping, processing or sharing the applicant's data. Marketing, privacy, subscription and agreement topics in an experience question do not count. The decision records `demoted_from`.
  - On a question that passes these checks, Jev's consent and attestation mass also pools with custom boolean, so a CONSENT/CUSTOM_BOOLEAN split is not `UNKNOWN`.
  - This is the one exception to "known sensitive semantic types are never downgraded". Text, checkbox, multi-choice and non-yes/no controls keep their consent and attestation types. A demoted question reaches the yes/no experience screener, which answers only from verified facts.

- **Saved-answer equivalence gates (round 7).** The wording decision now asks whether a truthful answer to the saved question is necessarily a truthful answer here: the same fact, scope, unit and timeframe, whatever the wording. A different unit (monthly), a different scope (only in the country where the position is posted) or an extra clause (base and/or OTE) is `NONE`. `CHOICE_PROMPT_VERSION` was `option-choice-v3` (round 9: `option-choice-v4`, with the new status instructions).
  - When exactly one same-type saved answer is offered, the shared type is the second signal. The decision is accepted at probability 0.90 and confidence 0.85, and the trace records `gate: type_anchored`. Live same questions landed at 0.85–0.94 on the old 0.95 gate.
    - Since round 9 this applies only to the low-stakes types in `TYPE_ANCHORED_TYPES`: referral, location, school, degree, pronouns and start date.
    - Work authorization, sponsorship, salary, EEO answers, relocation and every other explicit type keep 0.95 / 0.90, with `gate: standard`. A close wording can be a different legal answer: an H-1B holder's "currently authorized" Yes is not "authorized for any employer".
  - Untyped candidates keep 0.95 for the pick. When a pick among several untyped answers falls short, a separate single-candidate decision confirms it at the same 0.90 / 0.85 gate (`gate: confirmed`, purpose `question_confirmation`). Since round 9 the confirmation runs only for a custom-typed field; a typed field offered untyped answers keeps the standard gate.
  - The stated status (`work_authorization_status`) is never a wording candidate; only the derivation reads it.
  - An untyped saved answer whose question also has a typed answer (left behind by an import that typed the key) is not offered again.
- **Consent and attestation statements (round 7).** The person confirms up to five statements once, in simple answers: privacy notice, true information, contact, reference checks and background check. A CONSENT or ATTESTATION field is answered from them only when one Jev Choice (`statement`, purpose `statement_coverage`) finds the site's statement fully covered by exactly one saved statement of the same type, adding no further obligation.
  - The decision is gated at 0.95 with confidence 0.90.
  - Anything more holds with the statement quoted: no AI tools during interviews, a non-compete, arbitration, at-will employment, drug testing, or two obligations the saved statements cover only together.
  - Since round 9 a deterministic blocklist (`_ADDED_OBLIGATIONS`) holds such a statement before any call, with trace status `ADDED_OBLIGATION` and the obligation names.
    - It checks the statement's wording and options for drug screening, previous or current employers, non-compete, arbitration or waivers, AI tools, at-will employment, a background check, and extended screening (credit, driving record, fingerprints, social media, ongoing checks).
    - An obligation passes only when one of the person's saved statements of the field's type names the same kind. Only the background-check consent names a background check, and no saved statement names the others.
  - The answer is `SAVED_ANSWER`, carrying the person's own Yes or No.
  - A control the runtime cannot operate is never answered, and neither is a statement with no readable wording.
- **Salary period (rounds 7–8).** A select or radio whose options are all pay periods (at least two of hourly, weekly, monthly, yearly or annual) is answered from the unit the saved desired salary states. Its own wording must be empty or name a period. It must be typed as salary, or sit in the same form section as a salary field (live Lovevery: an unlabelled custom select next to "Desired Salary").
  - "Per year", "/yr" and "annual" give Yearly or Annual; "per hour" gives Hourly. With no stated unit it holds.
  - Core lets a salary saved answer back only such a pay-period choice of another type (`is_pay_period_choice`).
- **Work authorization from the stated status (round 8).** Matching work-authorization questions by wording fails for a sound reason: "authorized for any employer" is a stronger claim than "currently authorized". So with a stated `work_authorization_status` (closed vocabulary), a field typed WORK_AUTHORIZATION or SPONSORSHIP (by the heuristics, by Jev, or since classifier round 5 by the legal pool on a select or radio) is derived, not matched.
  - A table answers the obvious pairs without a call, for U.S. questions with no negation and nothing else asked (citizenship, visa type, clearance, another country, expiry). A citizen or permanent resident is authorized Yes, for any employer, and permanent, and needs no sponsorship now or in the future. `not_authorized` gives authorized No.
  - Otherwise one Jev Choice (`status`, purpose `status_derivation`) runs over the options, or Yes/No for a yes/no text question, plus `UNKNOWN`. Its state holds the status code and meaning and the two stated answers, and it picks the option that is the truthful answer for a person with exactly that status. The gate is 0.95 / 0.90.
  - The answer is `SAVED_ANSWER` citing the status. The trace (`status_derivation`: via, choice, status) never holds the status itself.
  - A question the status does not settle (security clearance, an unstated visa) falls back to the saved answers' wording path. Without a stated status nothing changes.
- **Review pass 4 on the derivation (round 9).**
  - **One question at a time.** The table answers nothing when the wording names sponsorship and also asks about authorization ("Are you legally authorized to work in the US? Do you need sponsorship?", "Can you legally work in the US …", "… or will you be requiring sponsorship?"). A bare "No" would say a citizen is not authorized. Jev reads such a question with an explicit rule: an option must be the truthful answer to every part, and a bare Yes or No true for one part only is `UNKNOWN`.
  - **The person's own legal answers win.** The person may have saved answers to the two canonical questions (`STATED_ANSWER_QUESTIONS`). If they contradict the status (`STATUS_CONTRADICTIONS`, the importer's table), nothing is derived; the trace status is `CONTRADICTS_STATED`, and the wording path decides. An example is a round-8 import of `ead_opt`, then "any EAD", with sponsorship No.
  - **The United States, as written.** "US" and "USA" count only in capitals, next to "U.S.", "United States" and "in the us", so the pronoun in "let us know" or "need us to sponsor" is not the country. Bare "America" no longer counts. The exclusions also cover passports, "another country", "outside the", "foreign", "international", and a longer list of countries and regions (Latin America, EMEA, APAC …).
  - **Options that name a status.** The table never picks among options that name a status of their own, such as "Permanent resident" beside "Citizen" or "Yes, I am a U.S. citizen". Its Yes/No answers need bare "Yes" and "No" options.
  - **More statuses.** asylee, refugee, daca, tps, pending_adjustment and dependent_ead join the vocabulary. The Jev state now carries each code's implications (`WORK_AUTHORIZATION_IMPLICATIONS`):
    - OPT and STEM OPT will need sponsorship in the future, so "now or in the future" is Yes.
    - An asylee or refugee never needs sponsorship.
    - DACA, TPS, a pending adjustment and a dependent EAD leave the future open.
    - For `other_visa` a sponsorship question is never derived, not even by Jev (trace status `NOT_SETTLED`). Only the person's own saved answer can answer it, by wording.
  - **The status as text.** A text question asking for the status itself ("Work authorization status") gets the status in the person's words ("U.S. citizen"), never the code.
    - The deterministic resolver and `stored_value` read the status through `saved_value`.
    - A select that asks for the status is derived from it, not mapped from its wording.
  - **The person's own answer comes first.** A saved answer for exactly this wording that does not fit the options is never replaced by a derived answer (status, statement, pay period, relocation). The field holds with the resolver's prompt, and the trace records `exact_saved_answer` `NOT_PLACED`. Only the verified address may still answer a residence question.
  - **Relocation names a city.** When the question or the chosen option names a city ("located in Austin"), the applicant's own city must appear there too, or the address answer holds (`CITY_MISMATCH`) and the saved relocation answer decides. A Dallas address never answers Yes to Austin.
    - "Washington, DC" and "New York, NY" count as cities.
    - "New York" alone is still read as the state.
- **Review items (round 7).**
  - The referral rule-4 fallback records min(confidence, 1 − `NOT_SOURCE`).
  - State codes count only in a comma or slash list of two or more, so a lone "OR" in a shouting label is a word.
  - Ungrouped facts that state the same kind of quantity (money, a duration, a percentage, a count of one unit) are consistency-checked unless both name different subjects.
  - A saved value with a control character holds its single-line field instead of stopping the step.
  - A CITY lookup types the bare city first (see "Lookup choice").
- **Runner events (round 7).** Each resolved step records `routing.trace`: the step, the classifier version, a projection of every field's route decision, and a projection of the traces added since the previous step. The projection keeps ids, stages, statuses, scores and page wording, and never fact values, drafts, review text or typed values. Review text is kept under `review_issues`, which the projection drops. A fill failure's `application.failed_retryable` event carries `failed_fields`: one entry per failed field with `field_id`, `label`, `status` and a redacted `detail`.

## Round 10: the imported answers reach the forms

A prepare-only rerun after the person imported `work_authorization_status`, `race_ethnicity` and a desired salary with a unit showed five places where a saved answer stopped short of the form (read from `routing.trace`). Each now has a deterministic rule or a gate that fits the evidence; nothing here produces a value, and every answer is still the person's own saved text or one of the site's own options.

- **Salary range selects (`salary_range`).** Lovevery's 119-option range select passed question equivalence (0.96) and then held on option equivalence (o0 at 0.58). Mapping a saved salary onto ranges is arithmetic, so `_mapped_option` now does it before any call (`ai/salary.py`): the saved value is parsed into one amount, its pay period and its currency ("USD 95,000 per year", "$45/hr", "95k annually"; `parse_salary` rejects ranges, approximate amounts, "+" and bare years), each option's bounds are parsed ("$50,000 - $59,999", "Under $20,000", "$200K and above", "$20 - $25 per hour", with currency signs, thousands separators, "k" and "m"), and the one range containing the amount in the same period is chosen.
  - The ranges' period is read from the options themselves (all agreeing), else from the field's label, help, placeholder and section headings, else from the bounds' magnitude: every finite bound under 1,000 is hourly, every bound of 10,000 and above is yearly. Monthly-looking bounds that state no period are not read (`UNIT_UNSTATED`).
  - The select is held, with no call, when the saved value states no unit (`NO_UNIT`), when the units differ (`UNIT_MISMATCH`), when the options or wording name another currency (`CURRENCY_MISMATCH`), when no range contains the amount (`NOT_CONTAINED`) or when the amount sits on a boundary two ranges share (`AMBIGUOUS`). The trace records the status, the range count, `unit_source` (`options`, `wording`, `magnitude`) and the chosen key, never the amount.
  - The rule applies to a single choice typed salary, or whose ranges name a currency; a years select with plain numeric ranges keeps Jev's option equivalence. Options that are not ranges but non-answers ("Prefer not to say", "Negotiable") are allowed beside the bands.
- **The pay-period select, whatever its label.** Lovevery's sibling period select (Hourly / Weekly / Monthly / Yearly) is labelled "Desired Salary" by the block heading, so it matched the saved salary question exactly and Jev mapped "USD 95,000 per year" onto "Yearly" at 0.86. A select whose enabled options are all pay periods is a pay-period choice whatever its label; it is answered from the unit the saved salary states before the exact-wording path, unless the person's own answer to that wording is itself a period ("Biweekly"), which is placed as their own answer (or held). The round-8 requirement that it be typed salary or sit in a salary field's section stays; the round-8 label-word check is gone.
- **Base salary wordings (superseded by the follow-up's `salary_derivation` below).** "What is your desired base salary?" was `NONE` (0.96) against the saved "What is your desired salary?". A plain desired salary states a base figure unless its value says otherwise, so a salary-typed field whose wording asks for a desired, expected, target, base, annual or yearly salary, pay, compensation, wage or rate is answered from `desired_salary` without a wording decision (a job-scoped salary answer for this job first, else the newest GLOBAL one).
  - It holds, with no call, for an OTE, on-target, total-compensation, bonus, equity, commission or package wording (`COMPENSATION_CLAUSE`), for a saved value that itself names OTE, total, bonus or equity (`SAVED_NOT_BASE`), for a monthly, hourly, weekly or annual wording the saved value does not carry (`UNIT_MISMATCH`; a saved value without a unit never answers a wording that names one) and for a currency the saved value does not name (`CURRENCY_MISMATCH`).
  - A numeric input (`type=number`) takes the bare amount only when the wording names the saved unit or the saved figure is annual, since a bare salary number reads as annual (`NUMBER_UNIT_UNSTATED` otherwise).
  - A current, previous, minimum or maximum salary, a salary range, a currency question or an explanation is not settled here and keeps the wording decision at the standard gate, as does any wording without a plain-salary marker ("What salary are you looking for?").
  - On a range select the base-salary rule hands the value to the range rule above, so "Expected annual base salary" over bands takes the containing band with `unit_source: wording`.
- **EEO answers map by type (`eeo_answer`).** "Race / For government reporting purposes …" offered two same-type candidates (`race_ethnicity` and `hispanic_latino`) and the wording decision landed at 0.77 to 0.78; "Veteran Status / If you believe you belong to …" hit 0.92 / 0.84 with one candidate. A self-identification field's semantic type carries the whole question, so EEO_GENDER, EEO_VETERAN_STATUS, EEO_DISABILITY_STATUS and EEO_RACE_ETHNICITY fields take the saved answer of the same type directly (a job-scoped one for this job first, else the newest; disagreeing newest answers hold with `CONFLICT`), with no wording decision, and the value is mapped onto the options through option equivalence at the usual 0.95 / 0.90 gate (an exact option needs no call). Optional self-identification fields are answered too.
  - For EEO_RACE_ETHNICITY the sub-answer follows the wording: a label naming Hispanic or Latino (and not race or ethnicity) takes `hispanic_latino`; a label naming race or ethnicity, or a combined "Race/Ethnicity" question, takes `race_ethnicity`; a label naming neither is read with its help text. The live "Race" label whose help text defines "Hispanic or Latino" is the race question. A wording whose sub-answer is not saved holds without a call (`SUB_ANSWER_MISSING`).
  - The person's own answer to exactly this wording still comes first, and a value that fits no option holds (`VALUE_DOES_NOT_FIT`) rather than falling back to wording. Only a field with no same-type saved answer at all keeps the round-9 wording path over the untyped answers, at the standard gate.
  - The round-9 rule for WORK_AUTHORIZATION and SPONSORSHIP is untouched: the stated status derives them, and their wording decisions keep 0.95 / 0.90.
- **Untyped reusable answers (`gate: untyped`).** The live travel question picked the right one of 17 untyped answers (q8 at 0.93) and the single-candidate confirmation then hedged (0.79). A pick among untyped saved answers (preferences such as travel, time zones, English proficiency, never an explicit-answer type, which imports typed) on a field whose type is not in `EXPLICIT_ANSWER_REQUIRED` is now accepted at probability 0.90 and confidence 0.85 (`UNTYPED_PROBABILITY`, `UNTYPED_CONFIDENCE`). A pick that falls short on a custom field still gets the single-candidate confirmation at the same 0.90 / 0.85. An explicit type offered untyped answers (a salary field with no typed salary answer) keeps 0.95 / 0.90 (`gate: standard`), and the type-anchored gate is unchanged. The trace now records `gate` before the decision, so a `NONE` outcome carries it too.
- **The work-arrangement preference (`work_arrangement_preference`).** Upstart's "Location Preference" select (remote / hybrid / on-site) was typed LOCATION and held, because an address never answers it. The simple-answers map gains the one-time key `work_arrangement_preference` (null default, free text, observed variants such as "Location Preference", "Preferred work arrangement" and "Remote, hybrid or on-site?"), imported as an untyped GLOBAL answer to `WORK_ARRANGEMENT_PREFERENCE_QUESTION`. A single choice whose enabled options are all work modes (remote, hybrid, on-site, in-office, in person, work from home …) or neutral preferences ("No preference", "Either"), with at least two modes named (`is_work_mode_choice`), is answered from it whatever the field's type, through an exact option or Jev's option equivalence. A label about the current or previous arrangement is not a preference. The verified address and the residence screener never read such a select; without the key it holds for the person.

### Round 10 (follow-up): the live prepare-only retry on the merged head

Read from projected `routing.trace` events after the merge of WP2 round 9, WP12 rounds 3–4 and WP7.

- **Salary derivation (`salary_derivation`), mirroring the status derivation.** Round 9 took SALARY_EXPECTATION out of the type-anchored gate, so each wording needed a 0.95 wording match and Jev rightly refused: eight applications held ("Desired base salary?" BELOW_GATE 0.67 then NONE 0.89–0.93, "What is your target salary for this role?" 0.59–0.65, "monthly salary expectation … (in $USD)" NONE 0.95–0.97, "desired hourly compensation" NONE 0.98, "desired annual compensation", "desired total compensation" and "be as specific as possible" text areas AMBIGUOUS 0.39, "Salary Range" NONE 0.76–0.86). A field typed SALARY_EXPECTATION (gate as before) is now answered from the saved desired salary by derivation, not by wording equivalence, replacing the base-salary wording rule above:
  - the figure with its period converted when the wording names a period (`convert_amount`: monthly = annual / 12, hourly = annual / 2080, rounded to the nearest 100 or, for an hourly figure, 1; the other way round from an hourly or monthly saved value): "$7,900 per month", "$46 per hour";
  - the figure itself, in the person's own words, for a base, target, desired or expected wording (`PLAIN`);
  - the figure as the minimum for a range or "minimum" wording (`MINIMUM`, also read from a field id such as `info.minimumDesiredSalary`), never an invented maximum;
  - for a total-compensation, OTE, bonus or equity wording (`COMPENSATION_CLAUSE`) the figure stated as the base salary ("$95,000 per year base"), and for any salary text area one sentence ("My desired base salary is $95,000 per year.");
  - a numeric input takes the bare amount in the stated unit or, unstated, an annual figure;
  - a current, previous or maximum salary, a currency question or an explanation is `NOT_DERIVED` and keeps the wording decision at the standard gate.
  - A saved salary without a unit still holds (`NO_UNIT`), as does a value that names OTE, total or bonus (`SAVED_NOT_BASE`), a range or approximate value (`UNPARSED`), a wording naming two units (`UNIT_AMBIGUOUS`) or another currency (`CURRENCY_MISMATCH`). The trace records the wording kind, the `period` chosen and any `conversion` ("year->month"), never the amount. The range rule converts too, so a yearly figure on hourly bands takes the band containing $46 and `UNIT_MISMATCH` no longer occurs.
- **Start-date buckets (`start_date_bucket`).** Lever's "Earliest Start Date?" (5 options, 4 applications) was VALUE_DOES_NOT_FIT 0.87–0.92 on the type-anchored gate, then option equivalence NONE: the saved `earliest_start_date` is a date or a notice period that names no option. A START_DATE select or radio is now bucketed deterministically (`ai/start_dates.py`): the saved value is read as days from today (a date in a common form, "immediately", "2 weeks", "two weeks after an offer is accepted", "4-6 weeks" by its later bound, "1 month" as 30 days) and the option whose stated range contains it is chosen ("Immediately", "Within 2 weeks", "2-4 weeks", "1-3 months", "More than 3 months", "Two weeks after offer acceptance"; the earliest bucket when two share a boundary); when no range contains it, the next later one; "Other" never, so a value later than every option holds (`NO_LATER_OPTION`). The free-text "When is the soonest you are able to start? Type here..." text area (START_DATE 0.99, AMBIGUOUS 0.52) is typed from the saved value in words ("October 15, 2026", or the notice period as saved). A text input keeps the type-anchored wording path. The person's own answer to the exact wording still comes first.
- **Status derivation on a five-option select.** "Can you work legally in the United States?" (JazzHR, 5 options) was ANSWERED o1 0.98 in one run and BELOW_GATE 0.91 in the next. When the option set holds a plain Yes and a plain No beside more specific options and the question is a plain authorization question (no sponsorship wording), the deterministic table answers the plain Yes for a permanent status; "work legally" now counts as authorization wording. A Jev decision within 0.05 below the gate (`STATUS_REPEAT_MARGIN`) is decided once more (the request carries `repeat: 2`, so the cache is not read); it must land on the same option and pass, and the lower of the two scores is the answer's confidence. The trace records `repeat`. Compound questions still never take the table, and legal and EEO types are still never type-anchored.
- **Authorization wording without a type.** Ashby's "Are you authorized to be employed in the United States?" (radio, Yes/No) reached routing with no semantic type (WP10 takes the classifier miss). A field typed UNKNOWN or CUSTOM_BOOLEAN whose wording is plainly a U.S. authorization question ("authorized to be employed", "legally able to work", "eligible to work"; no sponsorship, negation or other subject: `_plain_authorization`) now gets the status derivation from the heuristics alone. Sponsorship wordings, other countries and clearances do not.
- **`work_arrangement_preference` as a closed vocabulary.** The key takes `remote`, `hybrid` or `on-site` (common spellings are normalised; "Remote or hybrid" is rejected). Greenhouse's "Location Preference" checkbox group (typed LOCATION; remote / hybrid / office) is a work-mode choice too, so the options naming exactly the saved mode are selected without a call; when the options mix modes, Jev maps the code onto them. Ashby's "Base is a fully in-person work environment. This role requires working on-site in Austin …" (radio, LOCATION) is an on-site-in-a-named-city question (`work_arrangement`, `onsite_city`): derived from the key with `willing_to_relocate` and the verified city (round 9's city rule): on-site acceptable and (already in that city or willing to relocate) is Yes; on-site not acceptable, or not willing to relocate, is No; UNKNOWN (held) when the key is null, or the relocation answer is needed and null. The residence screener never reads either.
- **SMS consent.** "We may use SMS during the hiring process. Do you give us permission to text you …" (CONSENT) holds while `consent_to_contact` is null, as it should. Since round 11 it has its own statement key, `consent_sms_messages`.

## Round 11: saved answers that still did not reach the form

A prepare-only retry on 2026-09-25, after the person saved every reusable key, left saved answers and stated facts short of the form. The rounds 1–10 rules all hold: no table answer for a compound legal question, and legal and EEO types are never type-anchored.

- **A Yes/No into a text area.** A custom text area (CUSTOM_LONG_TEXT or another custom type) now takes the untyped wording candidates; a typed text area still asks for prose.
  - When the wording decision maps a saved Yes/No onto it, at the gates as before, the answer is one sentence in the person's voice. For "Have you signed any non-competition or non-solicitation agreement with any other employer? If yes, describe" with `non_compete_agreement` No, that is "No, I have not signed any non-competition or non-solicitation agreement." (`yes_no_sentence`, core).
  - A bare saved Yes never answers a question that asks for more ("If yes", "describe", "details", "please provide" …): the field holds with `YES_NEEDS_DETAIL`. A saved value with its own detail is typed as saved. Detail is never invented.
- **Conditional follow-ups (`conditional_follow_up`).** A question with a condition ("If yes", "If you were …", "… if your …") or a reference to the question above is a follow-up. It runs after the stored answers and before the route gate.
  - Its governing question is the preceding yes/no question (within six fields) sharing the most words with the condition ("referred" for "If you were referred by …"; the whole wording for "the government official mentioned above"), the nearest on a tie. Failing that, for "If yes" or "the above question", it is the immediately preceding yes/no question.
  - A negated condition ("If you do not have a bachelor's degree …", "If you answered no …") applies after a No and is never one. A condition no preceding question states ("If you are selected for this role …") finds no governing question.
  - When the governing answer is the person's saved No, the follow-up does not apply: "N/A" (or the site's one N/A option) when required, blank and never generated when optional (`LEFT_BLANK`). The answer cites the governing saved answer.
  - A visa or work-authorization follow-up ("details and expiration date here if your specific visa or work authorization …") does not apply to a stated U.S. citizen or permanent resident, and cites the status.
  - A governing Yes, an unanswered or unsaved governing answer, or no governing question keeps today's hold.
- **SMS consent and interview accommodations.** Two new keys: the statement `consent_sms_messages`, which the coverage decision considers like the other consents, and the free-text `interview_accommodations` ([simple-answers.md](simple-answers.md)).
- **Countries and states without a model call (`closed_vocabulary`).** A COUNTRY or STATE select is matched deterministically before any call, by name, ISO code or common alias ("United States", "USA", "US"; "United Kingdom", "UK"; "TX" or "Texas"). The flag, dial code and bracketed codes are stripped first, so the 244-option "🇺🇸 United States (+1)" is found at once.
  - Only when nothing matches does Jev decide. On a list longer than 100 options it sees the options sharing a word with the value plus 20 others (`BOUNDED`).
  - The residence screener reads a country or state select the same way first.
- **Transient provider errors.** Every decision the resolver makes goes through `_decide`. After a NETWORK or TIMEOUT failure it is sent once more (a second budgeted call), and a second failure holds with both in its reason. A lookup suggestion is not retried, because the site is waiting.
  - "Are you legally authorized to work in the country for which you are applying?" reads the job's country from the listing's location when that names the United States unambiguously, so the status table answers it. That means the country itself ("Remote - United States"), or a plain "City, ST" whose code is not also a country code ("Austin, TX").
    - "Tel Aviv, IL", "Toronto, ON, CA", "Chicago, IL" and "Tbilisi, Georgia" are not read so; the question goes to Jev.
- **Profile links (`profile_link`).** An untyped text question asking for one profile link that names LinkedIn among the acceptable values gets the verified LinkedIn URL. An example is "Professional profile link (LinkedIn, portfolio, or personal site)". When LinkedIn is not named, or not saved, and a personal site or portfolio is named, it gets the website.
  - A question asking for several links, about another person or about a past profile is not answered.
  - The wording rule is the route gate: at most 0.01 of the source mass may be on another person.
  - Core accepts identity provenance on such a question (`PROFILE_LINK_TYPES`) only for a full URL that is one of the identity's own. A saved URL without its scheme ("www.…") is left to the person (`NOT_A_FULL_URL`).
- **Stated facts over derived ones.** A fact the person stated (`user:` source) replaces a derived one (`derived:`) for the same key everywhere the resolver reads facts: evidence, consistency, retrieval and screeners. The total years keys count as one key. A stated total of 8 replaces a derived 5 and is never averaged with it.
- **Years thresholds (`experience_screener`, `via: years`).** "Do you have at least 8 years of total experience?", "5+ years of paid media experience" and "more than 7 years" (strict) are settled from the years facts without a call.
  - The answer is Yes when the fact for the area the wording names (or the total, when it names none) meets the threshold, and No when the total falls short.
  - A wording that names an area the facts do not cover is never answered from the total: the total is removed from Jev's evidence. So "at least 8 years of total experience in direct response marketing" holds unless a `years_experience.direct_response_marketing` fact meets it.
  - Some years thresholds are never read from the years facts:
    - one about age, residence, citizenship or tenure ("Are you at least 18 years old?", "lived at your current address for at least 3 years");
    - a question setting two thresholds ("8 years in total, including 3 in direct response"), which is Jev's. Since round 12b a second number standing on its own counts too, even without "years" after it ("5+ years of experience, including 2 in paid social", "teams of 10 or more"). A number inside a word (B2B, GA4), an amount ($1M, 50%) and another years mention ("in the past 10 years") do not.
- **Experience questions reach the screeners (item 9).** A required choice (yes/no, select or select-all) whose wording asks about experience, skills, platforms, years or something done is admitted to the fact screeners whatever its route or source confidence, unless it asks about another person. Live examples are "Have you owned paid social strategy …" at route 0.92 and "Do you have SEO AND GEO optimization experience?" at 0.84.
  - The screener's own gate decides, and the answer's confidence is at most the route's own.
  - Only a possessive subject ("Has your manager led …") marks a question about another person. "Agency" or "manager" as the applicant's own setting or role ("a performance marketing agency environment", "as a paid media manager") does not.
  - A text question keeps the route gates, so a narrative stays the writer's.
- **A YES without citations.** When the yes/no screener answers YES at the gate but names no supporting fact, a years fact of one year or more for the area the question names supports it. Meta, Instagram and Facebook are `meta_ads`; Google, PPC and paid search are `google_ads` or `paid_search` (see `AREA_SYNONYMS`).
  - A threshold in the question ("5+ years") must be met by that fact, and a stronger-than-stated claim never is.
  - Otherwise the decision is asked once more (`repeat: 2`), requiring the facts. A second YES without them is UNKNOWN.
- **The screeners' evidence.** Every years fact and every stated fact goes into a screener's evidence, and retrieval fills it to 16 (`SCREENER_EVIDENCE`, from the previous top 8). An example is the agency story facts behind "Have you worked in a performance marketing agency environment?".
- **Select-all platforms.** An option naming one ad platform (Google Ads, Meta/Facebook/Instagram, LinkedIn, TikTok, Microsoft/Bing, Amazon, programmatic/DSP, X, Snapchat, Pinterest, Reddit, YouTube) is selected without a call from the years facts (one year or more) or story facts naming it. An example is "Which paid media platforms have you directly managed?".
  - Office and analytics products are not ad platforms (Google Workspace, Data Studio, Cloud, Analytics; Microsoft Excel), and a story that denies any work is never deterministic support.
  - Only the remaining options go to Jev.
  - An option no fact names, or one Jev leaves undecided, is left unselected (`left_unselected`), never held.
- **The saved salary's shape (addendum 2).** The live `desired_salary`, masked as `NNN,NNN per year`, now parses whatever surrounds it.
  - The saved text is normalized first: compatibility characters are folded (a full-width comma, a no-break space), invisible characters are removed, digit groups split by a thin or narrow space are joined ("150 000"), and a currency glued to its figure is split off ("USD150,000").
  - "About", "around" or "~" beside one figure still states it. A range, a floor or ceiling ("at least", "+") or no figure still does not.
  - The derivation reads a job-scoped salary for this job first, then the imported `desired_salary`, then the newest other one.
  - A value that still does not parse holds with a prompt naming the saved key, and the trace records `UNPARSED` with `saved_key` (never the value).
- **Monthly salary wording.** The question's own period wins over its help, placeholder or section text, so "What is your monthly salary expectation … (in $USD)?" is monthly. The saved unit parses in more spellings: "per yr", "a yr", "p/a", "/annum", "130k yr", "per hr" and "per mo".

## Round 12: the person's standing answer policies

The person does not answer screeners one by one: at retry five (2026-09-25) they declined 84 open questions and stated standing rules instead. The simple answers' `answer_policies` section ([simple-answers.md](simple-answers.md)) holds one Yes or No per class of question, and each new question takes its class's answer by meaning, never by wording. A policy imports as an untyped GLOBAL saved answer whose question is the policy's own statement (`ANSWER_POLICY_QUESTIONS`, core `answer_policies.py`). An answer therefore cites it as `SAVED_ANSWER`, and a policy can back a field of any type. A policy never takes part in wording equivalence: `_wording_candidates` leaves it out.

- **When a policy is asked (`_apply_answer_policies`, a fourth pass).** It runs after the stored answers, the route gates and generative routing. A required field that is still open takes a policy decision only when all of these hold:
  - There is no input of the person's own and no saved answer for exactly its wording, even one that does not fit. The field is not held by a reworded saved answer that does not fit, by verified facts that disagree (a screener conflict, a same-key or canonical conflict, or the consistency check), or as ambiguous or unsupported.
  - Its type and control allow a class (`_policy_classes`):
    - A custom yes/no question may take any class: CUSTOM_BOOLEAN, CUSTOM_SELECT, CUSTOM_LONG_TEXT or CUSTOM_TEXT, and UNKNOWN only when Jev's semantic reading puts at most 0.05 on the explicit-answer types.
    - A statement typed ATTESTATION takes `certifies_truth`, `not_current_or_former_employee` or `sanctioned_locations` (the heuristics type "I confirm I am not located in … Cuba …" as an attestation). One typed CONSENT takes only `certifies_truth`.
    - A yes/no years threshold the heuristics typed YEARS_EXPERIENCE takes only `meets_experience_thresholds`. A yes/no question typed as a residence takes only `sanctioned_locations`.
    - A choice needs a Yes/No or True/False pair, or a label that is a yes/no question (graded yes options such as "Yes, some experience"). A text box needs a yes/no-question label. A checkbox always qualifies. Select-all questions never do.
  - The wording names no work authorization, sponsorship, visa or clearance (`legal_wording`), a compound legal question included, and the question is not about another person.
  - A class the field may take has a stated policy. Without any stated policy nothing is asked.

  Work authorization, sponsorship, salary, EEO, pronouns, relocation, start date and referral questions never take a policy: they keep their own paths.
- **One decision (purpose `answer_policy`, prompt `answer-policy-v1`).** One Jev request per field holds:
  - the Choice `policy` over the five classes plus NONE, at the gate 0.95 / 0.90. All five classes are always offered, so mass never moves into a class because another class has no policy;
  - the Choice `polarity`: SAME when a yes states the class's yes, REVERSED for "I confirm I am not located in …" or "I have never worked for …";
  - on a choice without an exact Yes or No option, the Choices `yes_option` and `no_option`. `yes_option` is the mildest affirmative: a plain yes, or "Yes, some experience", "Yes, as part of a team", never an option that states years or amounts;
  - on a text box, the nouls `details_if_yes` and `details_if_no`.

  The state carries the question, its options and the employer. The saved policies are never sent. The decision goes through round 11's `_decide`, so a NETWORK or TIMEOUT failure is sent once more before the field holds (round 12b).
- **Applying the class.**
  - **The whole wording is agreed to.** A certification, an employee or sanctions question, any statement (CONSENT or ATTESTATION) and any checkbox hold, whatever the class, when their wording adds an obligation or asks for another consent: answering them agrees to all of it.
    - `ADDED_OBLIGATION` uses the round-9 `_ADDED_OBLIGATIONS` list over the question and its options: drug tests, previous employers, non-competes, arbitration and waivers, AI tools, at-will employment, and background, credit, driving or ongoing checks. An example is "Have you worked for Mock Co before? By answering you agree to binding arbitration."
    - `OTHER_CONSENT` reads the question's own wording: consent, permission, authorization, opt-in, SMS or text messages, marketing messages, newsletters, promotional offers. An example is "I confirm I have never worked for Mock Co and would like to receive promotional offers."
    - An experience question on another control is not checked, because its topic may name AI tools or a background without adding an obligation.
  - `claims_experience_asked`: the saved answer.
  - `meets_experience_thresholds` applies to any experience question that sets a minimum number of years, whichever experience class Jev read, so a claimed Yes never exceeds the stated years. With Yes, the answer is Yes when the minimum is within the stated years and No above them.
    - Round 11's years screener runs first, and it settles most yes/no thresholds from the years facts. It answers Yes when a named area's fact (or, with no area named, the total) meets the minimum, and No when the total falls short. The policy decides what that leaves open: a named area without a fact that meets the minimum, while the total does, or a missing total.
    - Minimums read: "5+", "at least eight (8)", "5 or more", "a minimum of 4", "more than 7" (strict) and a bare "3 years". A timeframe ("in the past 2 years", "2 years ago") is not a minimum.
    - The stated years are the larger of the `years_experience` total and a `years_experience.<area>` fact whose area the question names. A stated (`user:`) fact replaces a derived one for the same key.
    - These hold with a prompt naming the policy: a range, an upper bound, an age, two different minimums or a second number standing on its own ("5+ years, including 2 in paid social") (`YEARS_UNREADABLE`), two stated values for one key (`YEARS_CONFLICT`), or no stated years (`NO_STATED_YEARS`). A number inside a word (B2B, GA4) or an amount ($1M, 50%) does not count.
    - With No, the answer is No.
  - `certifies_truth`: the affirmative option ("Yes", "True", "I agree", or the box checked). A statement whose own wording does not say what it certifies holds (`NO_TRUTH_WORDING`: true, accurate, complete …). Option labels such as "True" do not count, so a talent-community sign-up offered as True / False is never a certification.
  - `not_current_or_former_employee`: the saved answer (No; "No." in a text box). The person's Yes to `previously_employed_here` or `previously_interviewed_here`, the newest GLOBAL one or one saved for this job, contradicts it (`CONTRADICTS_SAVED`, core `POLICY_CONTRADICTIONS`), and the field holds.
  - `sanctioned_locations`: the saved answer (No). It applies only to a question that names a sanctioned place or sanctions (`NO_SANCTIONED_PLACE` otherwise), and never when the verified address is in one (`CONTRADICTS_ADDRESS`).
  - Placing the answer:
    - A choice takes the exact Yes or No option without a call, else the `yes_option` or `no_option` pick at the gate whose label does not say the opposite. Otherwise it holds (`NO_OPTION`).
    - A checkbox is checked or left unchecked. A required box the answer leaves unchecked holds (`INVALID`).
    - A text box takes "Yes." or "No.", unless the question also asks for details for that answer (`NEEDS_DETAIL`, above 0.05).
    - A free-text "If yes, describe" follow-up after a policy Yes is never a policy question, because its label is not a yes/no question. It holds unless the writer answers it from facts.
    - Round 11's conditional follow-ups first run before the policy pass, when the policy's question is still open. They run once more afterwards for the follow-ups of the questions the policy answered (round 12b, `_policy_follow_ups`). After a policy No the follow-up does not apply, exactly as after a saved No: "N/A" when required, blank when optional, citing the policy. After a policy Yes it keeps its hold.
- **Provenance and trace.**
  - The answer is `SAVED_ANSWER` citing the policy's saved answer, whose id names the policy (`answer_policy_<key>_…`). Its note names the policy and `user:simple-answers`, and for a threshold the years fact ids. The confidence is the lowest of the class, polarity, option and detail scores.
  - The trace (`answer_policy`) records the classes the field may take, the class and its scores, the policy applied, the polarity, the decision and the reference ids. For a threshold it also records `years_rule`: the minimum, its strictness and the fact ids, never a fact value.
  - A class that applies but cannot be placed replaces the field's prompt with one naming the policy. NONE, BELOW_GATE, NOT_ALLOWED, NO_POLICY and the guards leave the hold as it was.
- **Budget.** The pass adds two calls and USD 0.01 per candidate field to the form's allowance, once per step (`CallBudget.allow_calls`).

## Round 13: the work arrangement follows the job's metro

`work_arrangement_preference` holds one value, and the person's is "remote". So an on-site question for a job in his own metro got No, although in-person or hybrid work there is fine. The simple answers gain `metro_area`: the towns around the person's verified `city`, comma-separated, stored as an untyped GLOBAL saved answer to `METRO_AREA_QUESTION` (core `preferences.py`). With it set, the place of the work decides (`ai/metro.py`); without it, or when no place can be read, the preference decides as before. The metro area never takes part in wording equivalence.

- **Reading a place (`read_place`, `job_place`).** The metro is the person's city and the `metro_area` places; a state written beside a town is dropped from the list.
  - **IN_METRO:** a text names a metro place as a whole word, and no other state is written right after it ("Austin, TX", "Austin, Texas, United States", "Round Rock (Hybrid)"). "Austin, MN" and "Georgetown, DC" are OUTSIDE.
  - **REMOTE:** a job location that says remote, anywhere or work from home. A job with no location is remote too, by the owner's rule.
  - **OUTSIDE:** a text that names another place: a "City, ST", a US state, a country, or another capitalized place in a job location.
  - **UNKNOWN:** nothing readable ("Multiple Locations", "Hybrid").
  - A question's wording is read for the place it names ("on-site five days per week at the Austin office"; places after "at" now count). The job's location is read when the question names none. A question's "this is not a remote role" never makes a remote job.
- **On-site questions naming a city (`_metro_onsite`).** In the metro the answer is Yes, whatever the job requires. Elsewhere, or for a remote job, it is No unless the person's saved `willing_to_relocate` is Yes.
- **Remote / hybrid / on-site selects (`_metro_work_mode`).**
  - In the metro: the mode the posting states in its location or title ("Austin, TX (Hybrid)"), else hybrid, else on-site. A select-all question gets both hybrid and on-site.
  - Elsewhere, or remote: Remote.
  - A select without such an option holds (`NO_OPTION`).
- **Office lists (`_is_office_choice`, `_office_choice`).** A choice asking where the applicant would work (preference or office wording, no current or past wording) whose options are at least two places ("City, ST" or a state), plus work modes or neutral answers. An example is "Location Preference": Burlingame, CA / Columbus, OH / Austin, TX / New York City, NY / Remote.
  - It takes the office in the metro, the person's own city first (every such office on a select-all question), else the Remote option. Without either it holds.
  - Without a `metro_area` such a list keeps its earlier paths.
- **Relocation questions naming the person's city.** `_is_relocation_place` also admits a relocation question that names the applicant's own city: "If you are not currently based in Austin, would you be willing to relocate?". The relocation screener picks the option that is true for the verified address ("I'm based in Austin"). An option that is neither Yes nor No is accepted when it names the applicant's city; a Yes or No must still be Yes.
- **The preference becomes the fallback.** The importer gives `work_arrangement_preference` match phrases such as "Location Preference", so the factual pass copies it onto those questions by their wording. With a metro stated, that copy on a work-mode choice, an office list or an on-site question is decided again by the metro (`_preference_copy`). The copy stays only when the metro cannot decide. For those fields the preference no longer counts as the person's own exact answer.
- **Provenance and trace.** The metro answers are `SAVED_ANSWER` citing the `metro_area` saved answer. The relocation answer is typed, so it is named in the trace. The relocation-question answer stays `PROFILE_IDENTITY`, since it comes from the address. The trace stage is `work_arrangement`:
  - `question`: `onsite_city`, `work_mode` or `office_location`;
  - `metro`: the verdict;
  - `place_source`: `question` or `job`;
  - `place`: the place named;
  - `modes` and `posted` for a select, and `relocation` when it was read;
  - the status: `ANSWERED`, `NOT_DECIDED` (the preference then decides), `NO_OPTION`, `INVALID` or `NO_METRO`.

## Round 14: the stated years in the factual pass, on-site questions with no place, the job's location

- **The stated years win in the factual pass.** `prefer_stated` and the source helpers moved to `interviewmaxxing_generation.resolver`, below the browser package: `stated_by_person`, `derived`, `USER_SOURCE` and `DERIVED_SOURCE` moved with it. `ai/experience.py` re-exports the same names, so routing and the answer policies are unchanged.
  - The factual resolver (`_FieldResolver`) now reads `prefer_stated(candidate.verified_facts())`, so round 11's rule also holds in the pass `DynamicPacketResolver.resolve` runs first.
  - "How many years of experience do you have?" with a stated 8 beside a derived 5 copies "8", citing the stated fact, without a Jev call. "How many years of paid media experience do you have?" with a stated 7 beside a derived 4 copies "7".
  - Only a derived fact gives way. A resume total, or a second stated total, that disagrees still holds the question for the person.
- **On-site questions that name no place (`_is_onsite_question`).** Examples: "Are you able to work on-site three days a week?" and "This role is hybrid (3 days in the office). Are you comfortable with that?".
  - Such a question is a yes/no select or radio with:
    - on-site or hybrid working wording;
    - wording asking whether the applicant can or will do it (able, willing, comfortable, open to, …);
    - no place named.
  - With a `metro_area`, the job's location decides, through the same `_metro_onsite` as round 13:
    - in the metro, Yes;
    - elsewhere, for a remote job, or for a job with no location, No unless `willing_to_relocate` is Yes;
    - UNKNOWN (for example "Multiple Locations"), the question keeps its earlier route, exactly as without a metro.
  - The trace is stage `work_arrangement`, `question: onsite`, `place_source: job`, and the answer cites the `metro_area` saved answer.
  - Wording about something other than the regular way of working is never the metro's: an in-person interview, travel, training, onboarding, events, meetings, client or customer sites. Neither is a question about the current or an earlier arrangement ("currently", "previous"), or one that does not ask whether the applicant can ("Do you have experience working in a hybrid team?").
- **What a question names that is no place.**
  - The employer's own name is taken out before a question's places are read (`_without_employer`), for city questions too. Before round 14, "This role requires working on-site at Mock Co" was a city question placed OUTSIDE by the company's name.
  - A question's reading (`read_place(..., question=True)`) treats the person's own state and country as too broad to place the work. So "on-site in Texas" and "in the US" read the job's location, while "in Colorado" or "in Canada" is OUTSIDE.
  - Country codes count only in capitals ("US", "U.S.", "USA", "UK"), so "work on-site with us" names no country. A job location's reading is unchanged: "United States", "US" and "Texas" are OUTSIDE there, as in round 13.
- **Where the job's location comes from.** `PacketContext.job` is the stored job (`ApplicationStore.get_job`). Its `location` is written only by `bind_job_identity`, from the identity the page shows (`extract_job_identity`: a schema.org `JobPosting` with a single `jobLocation`, its `addressLocality`).
  - On the `prepare-batch` path the listing's own location is not carried: the inventory has no location column and only the URL reaches `apply` (the round 14 report lists the files and lines).
  - A job whose apply page shows no such posting therefore has no location, and the metro rule reads it as remote.

## Round 15: the first mass slice's holds that the person's answers settle

Batch `mass-20260925-a` held 12 of 25 applications. Most of the held questions are answered by what the person already stated, and seven were narratives the writer should have written.

- **Why-us questions reach the writer, whatever their source scope.** All seven narratives routed WRITER at 1.0, but the classifier scoped several as EXPLICIT_ANSWER or low-confidence HISTORICAL_OR_CONTEXTUAL, so `_routable` or `_writer_scope` held them. By the owner's rule (every saved job is a fit, and the writer makes the case), a motivation narrative is written whatever its scope: `_motivation_narrative` already did so, but its wording predicate missed these questions.
  - `interest_question` adds the missed wordings to `motivation_question`: "Why Dovetail & this role?", "Why did you decide to apply to this role at ClickUp?", "Tell us a bit about why you're applying to work at Yondr. What excited you about this role?", "Tell us about yourself & your interest in Smalls." These take purpose `motivation`, with no scope label and no `candidate_narrative` call.
  - An explicit preference ("Why are you interested in a remote position?") and leaving a role are never motivation.
  - The knowledge store's own predicate misses these wordings too, so retrieval is asked `"Why are you interested in this role? " + question`, and the facts are ranked by the job description like any motivation narrative.
  - The AI-use questions ("How are you currently using AI in your workflows?", "How do you use AI to 10x your output?") ask about the applicant's own practice. Their scope is WP10's to fix (round 15 report).
- **Sponsorship without a call.**
  - The person's own answer to exactly this wording is placed by its polarity (`_stated_sponsorship`): a plain Yes or No, or a statement ("I will require sponsorship" / "I do not require sponsorship"). A not-applicable option ("N/A - I am based in …") is never chosen.
  - For a person with no stated status, their canonical `requires_visa_sponsorship` answer is placed on a generic sponsorship question (`_generic_sponsorship`). Such a question names no other country, visa type or subject, and asks nothing else. The job's location names no other country either: saved jobs are U.S. or U.S.-remote, so a job without a location is not abroad.
  - With a stated status the derivation decides, as in round 8. The status table now reads such a generic question as a U.S. one and answers its statement options.
  - A status contradicting the answer never places it.
- **A salary with its currency.** "What is your target annual salary? Please include the currency." was NOT_DERIVED ("currency" was read as a question for the currency). "Include the currency" now asks for the figure with it: "$135,000 USD per year" (`render_salary_with_code`).
  - A salary saved without a currency takes the currency of the person's verified country (USD in the United States); another country without a known currency holds (`CURRENCY_UNSTATED`).
  - "Desired salary currency" and "Which currency …" stay not derived.
- **The current role (generation resolver).**
  - `CURRENT_COMPANY` / `CURRENT_TITLE` and the labels "Current/Most Recent Company Name" and "Current/Most Recent Job Title" read the `current_company` / `current_title` facts first.
  - Without any, they read the profile's one `experience` entry marked current, citing the verified facts it links.
  - Facts that disagree are never chosen between.
- **English fluency.** "Are you able to speak, read, and write English fluently?" reads the stated `english_proficiency`: Yes for native, fluent, bilingual, full professional or C1/C2, No for basic or limited, anything else for the person. A question about another language is never the English level's.
- **Pronouns.** The saved pronouns take the one option naming the same pronouns before any Jev mapping ("he/him" on "He/him/his" or "He / Him", never "He/They"). Two such options, or a mixed set, leave it to Jev.
- **Middle name and time zone (new simple-answers keys).** Neither was stated anywhere the resolver could read.
  - `middle_name` answers "Middle Name — write N/A if none"; "none" is stored as "N/A".
  - `time_zone` answers "What is your Time Zone?" (Eastern / Central / …) on the option naming the same zone ("Central Time (CT)", "America/Chicago" → Central).
  - Without `time_zone`, an identity-typed question (LOCATION) reads the verified address (`address_time_zone`: Austin, TX is Central, El Paso Mountain). A custom select cannot carry an address-derived answer (core's provenance rule): it waits for `time_zone`, or for the classifier to type it LOCATION.
- **Platforms and AI-use select-alls.**
  - "Have you managed your own ads in the following platforms?" already selected the platforms the years facts state (round 11). When the decision about the other options, or retrieval, fails, those selections now stand and the rest are left unselected instead of holding.
  - "Which of the following best describes how you use AI in your current role? (Select all that apply)" is experience wording now ("how you use"), so it reaches the select-all fact screener.
- **Buckets and scales without a decision.**
  - A single choice of year ranges takes the range containing the stated years of the area it names. "Planning / buying media" and "media buying" are paid media (`AREA_SYNONYMS`): "…in planning / buying media for a national brand" (0-4 / 5-7 / 8+) is 5-7 for a stated 7.
  - A proficiency scale about one ad channel no verified fact names ("How would you rate your proficiency in CTV buying?") takes its one no-experience option, for a person who states their platform years. The answer cites those facts; a scale without such an option waits.
  - Neither rule applies to a skill that is not an ad channel ("Excel").

## Narrative escalation

Jev classifies and selects facts; it never authors prose. `COPY_KNOWN` describes responsibility, not ready-to-fill status. Readiness requires an actual verified source and canonical validation. `EXPLICIT_ANSWER`, unclear or low-confidence source applicability blocks both copy and writing unless an exact scoped user/saved answer already resolves the question. Demographic answers require an explicit verified saved answer; identity never implies them.

A narrow identity clarification handles near-threshold current-applicant source scores: it requires an existing canonical identity answer, high-confidence literal `COPY_KNOWN`, and a leading `APPLICANT_CURRENT` score and confidence of at least 0.90. One source-only Jev call receives the full fresh form context, not the candidate value. It must independently reach 0.95 before the local value is released. Historical, other-person and explicit-answer scopes remain held. For LinkedIn, GitHub and personal-website URL fields, and (round 4) for the applicant's email, phone, first, last, full and preferred name, near-threshold probability mass that merely moved from current to historical is accepted without the extra call. The combined applicant mass must be at least 0.95, with at most 0.01 in total on the other scopes. These values identify the applicant in either timeframe, while another person's datum (a supervisor's, reference's or emergency contact's email, phone or name carries other-person mass), an explicit answer or an unclear subject still gets the strict call. So does a contact or name question worded about a past or other identity, because that datum differs from the current one. The guard words are: previous, prior, former, past, maiden, birth, other, another, alias, aka, also known, used to, and since round 6 also old, earlier, original, legal, nickname, and "last" when it is not "last name" ("Email at your last employer" stays strict, "Last name" does not). The trace status is `APPROVED_TIMEFRAME_INSENSITIVE_URL` or `APPROVED_TIMEFRAME_INSENSITIVE_CONTACT`.

For prose, the optional [RAG runtime](rag-writing.md) retrieves up to eight current verified candidate facts using the question and the exact job description. It retrieves employer evidence only for the candidate and normalized application URL, with a separate namespace for optional style samples. Without RAG, Jev selects relevant verified facts. Work history does not establish motivation or personal intent. Before writing, consistency checks include canonical counterevidence outside the retrieved set, including differently named keys for the same subject. Generic experience collections may contain different compatible employers, budgets and team sizes.

The configured Opus writer receives those candidate facts, scoped job evidence, style samples, the question and length limit. It receives no browser bindings. Personal claims cite candidate facts; employer claims cite job evidence; style samples cannot establish facts. Opus returns a strict JSON object with `status`, paragraph-aware `sentences` and `missing_information`. `READY` requires supported sentences and no missing information. `NEEDS_INPUT` names the missing details and contains no draft sentences. For a broad summary, relevant experience is enough without requiring a complete life history. Unknown or cross-namespace citations, extra schema fields, overlong text, incomplete responses, tool calls and unexpected model IDs are rejected.

Jev checks each sentence against its cited sources and checks completeness against the whole question. Support probabilities at least 0.95 pass; probabilities at most 0.05 hold. Intermediate narrative consistency or grounding results receive an independent structured Opus review, which must return `SUPPORTED`. Conflicts, incomplete answers and unsupported claims remain held with specific issues. Consistency review sees all current verified candidate facts; draft review checks the actual cited sentences and scoped job evidence. Reviews use the same provider budget. Missing ABM platform history is never inferred from general B2B experience.

Structured draft-review `UNSUPPORTED` or `INCOMPLETE` results may receive one corrective rewrite with the same evidence and bounded review feedback. Both the failed and corrected attempts stay in the private trace. The corrected draft must pass fresh Jev and Opus checks. `NEEDS_INPUT`, conflicts, invalid citations, malformed/provider failures and exhausted budgets do not retry; a second rejected draft stays unresolved.

Semantic classification and routing require confidence >= 0.90 and selected probability >= 0.95. These checks remain probabilistic; they are not proof that arbitrary prose is true. An uncertain prose responsibility can escalate to WRITER, but source/sufficiency/grounding gates still apply. Packet confidence is the conservative minimum of applicable model gates and support scores; it is not calibrated probability of truth. An unsupported sentence becomes a user-input hold, and all real work remains prepare-only for review.

## Stories, effort and the no-slop rewrite (WP12)

The candidate's own professional stories are a fourth knowledge kind next to facts, job
descriptions and style samples ([rag-writing.md](rag-writing.md) describes indexing,
chunking and fact extraction). For a WRITER-routed field the resolver's retrieval is
marked `narrative`, and the store adds up to four story chunks ranked by the question,
the job title and the description's key requirements; screeners, exact facts and bare
identity fields never get them, and a retrieval that returns stories for a
non-narrative field is rejected. Story chunks are validated like job evidence (content-
hash ids `story:<sha256>`, unique, outside the fact and job namespaces, labelled with a
title) and travel to the writer in the facts list under `key: story`, so a sentence
cites them in `fact_ids` like a fact. Grounding treats a cited passage as that
sentence's evidence: every claim still needs a citation that supports it, and an
unsupported claim holds the field. A chunk that contradicts a verified structured fact
holds before writing (`story_consistency`, cached per runtime). Provenance keeps citing
verified fact ids only; the story ids go into the note, and traces carry ids and scores.

Writer effort is per purpose (`--writer-effort`, default `high` for cover letters and
narrative answers; reviews stay `low`), recorded in receipts and in the `provider.budget`
event (`reasoning_effort` by purpose and `writer_effort` as configured). After a draft is
grounded, the runtime rewrites it under the no-AI-slop rules and grounds the rewrite
again, keeping the grounded draft whenever the rewrite drops a citation, adds a number,
drifts in length or fails a check; a residual lint pattern allows one more rewrite. The
pass adds one or two Opus calls and one or two Jev grounding calls per narrative field,
which is why the per-WRITER-field allowance was raised to 12 calls and USD 0.30 (24 calls and USD 0.75 since September 25, after a live narrative exhausted 12 calls before its draft); a call the
budget still refuses keeps the grounded draft rather than holding the field.

**Round 3 (WP12).** Narrative calls (writing, motivation, cover letters, the no-slop
rewrite) send an explicit reasoning budget by effort (`reasoning.max_tokens`: low 1024,
medium 1536, high 2560, xhigh 5120, max 10240) and a request `max_tokens` of that budget plus
the purpose's answer allowance (2000 tokens for an answer or motivation, 3000 for a cover
letter or a rewrite), so the answer keeps its whole room; with `effort: high` OpenRouter
had reserved about 80% of a 3000-token limit for reasoning and cut answers. A `length`
finish reason is retried once at the same effort with a larger budget (half more reasoning,
twice the answer room); the draft trace's `attempts` list records each call's status,
finish reason and budgets; a second cut, or a retry the call budget refuses, holds with
both reasons. Reviews keep `reasoning.effort` (short verdicts). The reservation for a
high-effort writer call grows from 0.06 to 0.09-0.11 USD plus the body (0.16-0.20 on the
retry); the allowances are unchanged. Interest, motivation and "why us" questions are now
their own purpose, `motivation` (trace status `MOTIVATION_PURPOSE`): the reason is the
alignment between the job's cited requirements and the applicant's cited experience, a
`career_motivation` fact (docs/simple-answers.md) is added to the evidence and may be
cited, and no personal reason is demanded. The yes/no, choice and select-all screeners
(prompt `experience-screener-v2`) read the derived `years_experience.<area>` facts (one or
more years is experience in the area, including having managed or used the platform it
names) and story facts (employer type, platforms named) as evidence, mapping onto options
through the existing per-option source decision; choice and select-all screeners retrieve
with their option labels. Enumeration questions ("How many direct reports do you currently
manage, or have you managed…") carry a guidance rule to the writer: present each item the
facts state with its size, employer and dates, never hold for completeness, and no
totality words unless a fact states the total; a draft with one gets the one corrective
rewrite (trace status `TOTALITY_REJECTED`).

**Round 4 (WP12).** Provenance: story facts and per-area years facts are written
`UNVERIFIED` by the index script and become evidence only once the person confirms them
through `scripts/rag_answers.py import-facts` (the review's `.confirm.json`); the total
years fact stays verified; areas come from role titles or bullets that state their own
duration, never from a mention or a linked story ([rag-writing.md](rag-writing.md), Round
4). Motivation narratives need the applicant's own reason, a cited story passage or the
`career_motivation` statement, or hold before writing; a draft citing neither gets the one
corrective rewrite (`MOTIVATION_UNCITED`; superseded in round 5, addendum 2). The consistency comparison set tiers same-key
and global claims before the count-ranked rest (`tiered_first`), for the Jev check and the
review. The humanizer keeps each sentence's citation set together (`REJECTED_MOVED_CITATION`)
and every humanized draft gets the independent review. The form allowance is granted once
per step per runtime, so a re-resolve of a step grants nothing more.

**Round 5 (WP12).** One story sentence is one piece of evidence: when the story consistency
check drops a chunk, the story facts extracted from it (their `story:<chunk id>` source, or
their sentence in the chunk's text) leave the field's evidence too, and a story fact the
fact consistency check drops takes the chunks that carry its sentence; the propagated ids
are traced under `story_evidence_dropped` with `propagated_from` (review pass 5, M1). A
strong review cached for the candidate revision no longer answers a later field that
selected a story fact: its comparisons run on the cached Jev verdicts, so the story fact
dropped for the first field is dropped again, and the cached review stands in for a second
review (`consistency_cache` with `dropped_story_fact_ids`; M2). The story verdict cache
stores a verdict (its score and its asks flag) only when two slots fit, and reads it only
whole (L4). A draft that copies more than 12 consecutive words of the person's
`career_motivation` gets the one corrective rewrite (`STATEMENT_QUOTED`; every
deterministic finding of a draft goes into that one rewrite, `rejected_for`), and a
humanized rewrite that does is rejected (`REJECTED_QUOTED_STATEMENT`). Story linking and
dating changes are in [rag-writing.md](rag-writing.md), Round 5. Addenda: fit is given for every saved
job, so cover letters, motivation and narrative answers build the case and never hedge or judge
fit (`FIT_GIVEN_RULE`; a hedge, disclaimer or fit comment gets the one corrective rewrite,
`FIT_HEDGED`); round 4's personal-reason demand is superseded (the alignment is the reason); the
independent draft review judges grounding and consistency only; a review hold names the profile
fact ids it referenced; and a WRITER question asking to calculate or analyse data given with it
is answered under the purpose `case_analysis` from that data alone, working checked in code,
provenance `GENERATED_FROM_QUESTION`, or held with "The table referenced is not in the recorded
question" when the recording carries no data.

**Round 6 (WP12).** Cover letters follow the owner's rubric ([rag-writing.md](rag-writing.md),
Round 6): the employer from the job record; facts retrieved per key requirement (at most two
each, figures first, no twin claims, one years fact at most and none below the posting's ask,
up to 12 for a letter); the whole description up to five chunks; story passages dropped only
at p <= 0.05 with the uncertain band reviewed once, and the passage that best matches the
first priority listed first; a rubric-shaped writer prompt whose code-checkable lines
(`GREETING`, `LETTER_LENGTH`, `OPENING`, `CLOSING`, `STORY_MISSING`, `JOB_RESTATED`,
`EMPLOYER_NAME`) get corrective rewrites; one independent review per draft for its grounding
and the rubric's HARD lines together (`letter_review`), with one improvement pass that never
costs a grounded letter; a humanizer that skips a clean draft, may delete or fold job-only
sentences, lints the genre and retries a rejected rewrite with its reason, never discarding
one silently. Citation ids travel as short aliases on the writer's wire, Jev grounding and
consistency requests are batched under the request bound, and a cover letter has its own call
allowance (`FORM_LETTER_*`).

## Bounds and observations

Production budgets scale with the form (round 6). Each resolved form gets 24 calls and USD 0.30, plus 24 calls and USD 0.75 per `WRITER`-routed field (8 calls and USD 0.15 at first, 12 and USD 0.30 for WP12's story consistency check, no-slop rewrite and second grounding, then 24 and USD 0.75 after a live narrative exhausted 12 calls), on top of what the runtime already used, capped at 200 calls and USD 4.00 in total (`CallBudget(scales_with_form=True)`, set by `build_ai_runtime`). The runner's `provider.budget` event records the limits used. A budget without that flag keeps fixed limits, and its defaults share one per-runtime budget: 48 provider calls (raised from 32 when option-equivalence and lookup-suggestion decisions were added; each is one cheap Jev call), USD 0.50 of conservative reservations and 60,000 request bytes. Jev uses a 15-second timeout; the writer uses a 90-second timeout and at most 3,000 output tokens. Strong review allows at most 1,200 output tokens. Runtime embedding requests reserve and record costs in the same budget before HTTP. Each provider call has one attempt. The router batches the whole form (default 16, configurable 8/16/32 in the historical benchmark), recursively splits oversized requests without dropping context, and handles at most 100 fields, fact routing at most 40 verified facts per bounded comparison, and the writer at most eight relevant facts. Exceeding a bound holds; it never silently truncates candidate evidence or retries indefinitely.

**Concurrency within a form (round 5).** After the single batched full-form classification, `DynamicPacketResolver` resolves independent fields concurrently. It works in three passes, each finished before the next:
1. stored answers: referral policy, option equivalence, reworded saved answers and residence;
2. the route gates, with their identity clarifications;
3. generative routing: screeners, fact screeners, exact facts and narratives, each with its retrieval, consistency check, writing, grounding and any Opus review.

Each field runs in a worker thread, at most `max_concurrency` at once (default 3). Items start strictly in form order (round 7), so the form-order consistency step never waits on a field that has not started, whatever the semaphore's fairness. The limit also bounds concurrent provider calls, rate limits and memory. A run cancelled mid-pass waits up to 10 seconds for the calls still in flight, and records their cost before it releases the application (round 7). `max_concurrency=1` is the old one-by-one order. The runner's lookup suggestions for a step are decided the same way, through `choose_suggestions`.

Results, `narrative_traces`, retrieval receipts and provider receipts are buffered per field and appended in form order, so they match a sequential run. Fields enter the fact-consistency check one at a time, in form order, so shared verdicts and the Opus evidence review are asked and reused exactly as in sequence while the writers still run in parallel. Identical Jev requests in flight share one call (single-flight in `BoundedDecisions`).

The budget is the same shared one. A refused call (call count, cost or request size) holds only the field that made it, and the others complete. When the budget runs out partway through a form, which fields run out first can vary with timing. An offline benchmark in `tests/browser/test_rag_routing.py` checks that three narratives at 0.3 s per provider call take less than twice as long as one.

Reservations use UTF-8 request bytes plus framing as a conservative input-token allowance and the writer's output-token cap, with the verified model prices below. Failed/unknown-cost calls retain their reservation. Receipts separately record requested/resolved model, purpose, actual provider latency, reported cost, reservation and status. Aggregate cost stays null when any call has unknown cost; a known subtotal and unknown-cost call count are reported separately. Reservations are a client-side estimate, not a provider-enforced billing limit. Browser latency is not included. The bounded in-memory decision cache holds at most 128 entries, never persists raw private profile values, and is not shared across processes. Durable application/resume/duplicate state remains the canonical store's responsibility.

Official contracts checked September 23, 2026:

- [OpenRouter Jev 1.13](https://openrouter.ai/typesafe/jev-1.13/api) describes the typed decision model, priced at USD 0.042 per million input tokens. The existing selection client correctly uses `POST /api/alpha/decisions` with `{model,state,questions}`. The [official provider SDK changelog](https://github.com/OpenRouterTeam/ai-sdk-provider/blob/main/CHANGELOG.md) also identifies that endpoint.
- The [OpenRouter Jev guide](https://openrouter.ai/docs/guides/community/jev), [TypeSafe confidence](https://docs.typesafe.ai/confidence) and [Noul primitive](https://docs.typesafe.ai/primitives/noul) describe the decision contract and score semantics.
- [TypeSafe Choice](https://docs.typesafe.ai/primitives/choice) defines choices, criteria, confidence and probabilities. Jev is not sent to chat completions and need not appear in the ordinary chat-model catalog.
- The [live OpenRouter model registry](https://openrouter.ai/api/v1/models) included exact `anthropic/claude-opus-5.5`, structured-output support and USD 4 / 20 per million input/output tokens. The writer uses [OpenRouter structured outputs](https://openrouter.ai/docs/guides/features/structured-outputs), no alternate-model list and no provider fallback.

## Historical v6 verification receipt and limits

The following frozen results predate the RAG and strong-review additions described above. Current RAG evaluation receipts are kept separately under `.imx/rag-writing/`; they do not replace or change these historical measurements.

The frozen source is `full-form-routing-v6`; exact file hashes and configuration are in `.imx/dynamic-applications/astra/frozen-v6.json`. Thirty-five targeted unit tests pass, with Ruff and strict mypy clean. Coverage includes current versus supervisor/history sources, explicit writer scope, saved-answer scope mismatch, autofill versus attachment, conflicting writer evidence, unsupported claims despite valid citation IDs, conservative confidence, missing costs and stale binding/cache contexts.

The independent v6 synthetic source-confusion challenge produced zero unsafe actual copies across 17 cases and correctly identified both narrative questions. Only two of four intended known-copy positive controls were ready: conservative confidence held first-name/email controls. This is a safety observation on a small synthetic suite, not a completion guarantee.

Final real-form accuracy is scored independently from frozen v009 heldout inputs. The corpus contains 509 unique real questions, 79 real narratives and 28 observed backends, plus 52 separately identified synthetic cases. The mapper's broader backend inventory is a separate artifact. Full-form timing includes semantic routing and readiness checks; provider call latency and browser navigation/inspection latency are separate measurements. Development runs compare batches 8/16/32 with two cold repetitions; v4 results are superseded by the source/purpose gates. The v5 8/16/32 comparison selected batch 16; v6 keeps those classifier thresholds and changes only narrative sufficiency, with separate v6 confirmation and heldout timing.

Independent v6 scoring preserves responsibility and execution readiness separately:

| Frozen split | Unique questions | Actual known-copy readiness | Real narrative responsibility recall | Unsafe actual copies |
| --- | ---: | ---: | ---: | ---: |
| Development | 441 | 66/119 (55.5%) | 58/62 (93.5%) | 0 |
| Blind heldout | 120 (109 real, 11 synthetic) | 10/21 (47.6%); real 10/20 (50%) | 15/17 (88.2%) | 0 |

The heldout pre-frozen substantive narrative subset was 12/12; synthetic narrative recall was 2/2. Two heldout narrative labels (“Headline” and “How technical are you”) were conservatively ambiguous. No narrative was routed to copying. The zero-unsafe result is an observed sample outcome, not a guarantee. Many valid basic fields still hold; the implementation does not claim full known-fact readiness. Confusion matrices and denominators are in `evaluation/v009-{development,heldout}-frozen-v6.score.json`.

The heldout run covered 310 observations across 30 full forms: median 0.409 seconds, p95 1.294 seconds, 18.17 fields/second. Its 43 provider calls reported USD 0.017690652 known cost plus one unknown-cost malformed response, so total cost is null. Development had one malformed call among 117; heldout had one among 43. These failures safely held affected fields without retry. Two unchanged development-only replays passed (31,641-byte request, 23 questions and 23 valid answers), so no deterministic client/size bug was reproduced. The original malformed response detail was not retained; a transient cause is not proven. Frozen metrics were not replaced with replay results.

The isolated localhost fixture uses public employer wording with fictional approved facts and the actual runtime with submission disabled. Under v6, AutoRaptor's “Tell us about your experience” produced a `GENERATED_FROM_FACTS` packet on the first attempt, with no canonical problems and observed text filled locally. Sentence grounding scores were 0.97/0.96/0.96; packet confidence was 0.96. Exact Opus 5.5 took 4.135 seconds and reported USD 0.007816; the entire fixture took 5.029 seconds. This is one observed success, not a latency guarantee.

A separate Canals motivation question with only fictional work history stayed blank: the source gate held it, and a direct Opus sufficiency probe independently returned `NEEDS_INPUT` (5.321 seconds, USD 0.008688). Both localhost fixtures verified `submit_dispatched=false` and zero submission events. These are synthetic candidate/fact tests using real public wording, not real candidate applications. Receipts/screenshots are in `.imx/dynamic-applications/astra/grounded-fixture/` and `unknown-intent-fixture/`.

The earlier v5 complete fictional examples were conservatively held by an overly broad Jev coverage question before writing. Those development-only failures led to the separately frozen v6 typed writer sufficiency contract; no blind labels or outcomes were used to change the implementation. Earlier component-only Opus acceptance is recorded separately in `writer-component-receipt.json`.

The root integration owns browser lease safety, fresh document/control/action reinspection, final-review stopping and broader runtime regressions. AI-module and offline routing checks alone do not establish complete application execution.
